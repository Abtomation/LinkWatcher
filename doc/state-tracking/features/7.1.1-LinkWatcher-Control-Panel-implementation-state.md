---
id: PD-FIS-056
type: Process Framework
category: Feature Implementation State
version: 1.8
created: 2026-06-29
updated: 2026-08-17
description: "Feature implementation state for LinkWatcher Control Panel"
feature_id: 7.1.1
feature_name: LinkWatcher Control Panel
workflows: WF-010
status: COMPLETE
---

# LinkWatcher Control Panel - Implementation State

> **📖 Craft home**: the [`feature-implementation-planning` skill](../../../.claude/skills/feature-implementation-planning/SKILL.md) — initialization and living-document maintenance craft, activated from Feature Implementation Planning's Check Recommended Skills step.
>
> **🚨 NEVER archived**: instances of this template are permanent feature documentation for the feature's entire lifecycle — finalization sets status `COMPLETE` in place (policy consumed by Code Review, Implementation Finalization, and Release & Deployment).
>
> **Retrospective Analysis mode** (onboarding tasks [PF-TSK-064](../../../process-framework/tasks/00-setup/codebase-feature-discovery.md), [PF-TSK-065](../../../process-framework/tasks/00-setup/codebase-feature-analysis.md), [PF-TSK-066](../../../process-framework/tasks/00-setup/retrospective-documentation-creation.md)):
> - Section 3 tracks analysis progress rather than planned tasks
> - Section 5 (Code Inventory) is the primary deliverable — every file must be assigned
> - Section 7 documents decisions discovered in code, not planned decisions
> - All content is descriptive ("what is") rather than prescriptive ("what should be")

---

## 1. Feature Overview

### Feature Description

The LinkWatcher Control Panel is the project's **first GUI surface**: a single-window Tkinter/ttk desktop application that acts as a *pure supervisor* over the LinkWatcher daemons running across every registered project. LinkWatcher daemons run silently in the background, one per project, started automatically by a Claude `SessionStart` hook. Today an operator has no unified way to see what is running, stop a daemon, read its log, edit its configuration, or run a validation scan — they must inspect OS process lists and open log/config files by hand.

The panel provides one desktop window to see and control all daemons across projects: list them with live status, start/stop them, tail their logs, view/edit each project's configuration, trigger link validation, and drain-then-terminate all managed daemons when the window closes. The window also surfaces itself automatically whenever a daemon is auto-started by the hook, so the operator always has a visible handle on LinkWatcher activity.

Architecturally, the panel introduces **no new daemon-side contract**. It manages daemons entirely through existing external surfaces — the lock file, OS process state, the launcher script, the `--validate` CLI mode, log files, and the per-project YAML config — which keeps it decoupled from the daemon and crash-safe (panel failure never harms a running daemon).

### Business Value

- **User Need**: Manage link maintenance across multiple projects from one window, without hunting through process lists and log files.
- **Business Goal**: Make LinkWatcher activity visible and controllable; pair every hook-started daemon with a visible operator handle.
- **Success Metrics**: An operator can identify and control every running daemon from one window; panel-initiated shutdown produces zero corrupted files; start/stop/log/validate/config-edit are all achievable without the command line.

### Scope

**In Scope**:

- Live daemon list across all registered projects with truthful status (running / stopped / stale-lock / starting / draining / force-stopped)
- Start (via the existing launcher) and stop (drain-then-terminate) of individual daemons
- State sync with reality (daemons started/stopped outside the panel, e.g. by the hook)
- Live log tailing, on-demand link validation, and per-project config view/edit with validate-on-save
- Drain-then-terminate-all on window close, bounded by a grace period (can never hang)
- Single-instance behavior + hook auto-open/surface of the one window
- Installer + hook-wrapper changes to ship and auto-open the panel

**Out of Scope**:

- A cooperative graceful-stop contract (**stop-sentinel** a daemon polls) — deferred; MVP uses bounded drain-then-force-terminate
- Any new API/daemon-side contract, and any database/schema
- Automatic daemon start-up ownership (the `SessionStart` hook remains the automatic start path)
- Performance tests and full E2E acceptance (routed to their own downstream tasks)

---

## 2. Current State Summary

**Last Updated**: 2026-08-18
**Current Status**: **Code Review gap closure complete (Feature Enhancement, PF-STA-111) — awaiting re-review.** All 19 product-path findings from the 2026-08-17 review (CR-2 … CR-20) are fixed, tested and mutation-verified across three sessions; the feature returns to `👀 Needs Review` for the standalone re-review of record. **CR-1 is deliberately not closed**: the hook auto-open lives on the framework path, where a local edit is reverted by the next rollout, and is tracked as **PF-IMP-2032**. FR-8, UI-7, BR-8 and the first clause of AC-11 therefore remain **UNMET** — this feature must not reach `🟢 Completed` until that IMP lands. Per-finding outcomes with their verification evidence are in §10; the root cause behind CR-3 was routed outward as [PD-BUG-123](../permanent/bug-tracking.md) against feature 0.1.3. The frontmatter `status: COMPLETE` refers to the *implementation* close-out and stays as finalization set it; the live gate state is the feature-tracking Status cell.
**Current Task**: — (gap closure closed out; next is the standalone Code Review re-review, then Performance & E2E Test Scoping)
**Completion**: Phases A–G done + finalized; implementation 100% (7 of 7 phases). Review gaps: **19 of 20 closed**; 1 open (CR-1) and blocked on PF-IMP-2032. Suite: 1324 passed, 3 skipped, 4 xfailed; panel suite 314

### What's Working

- [✓] All design documentation complete and approved (FDD, UI Design, TDD, Test Spec, Implementation Plan)
- [✓] **Phase A (Foundation & settings)** — `control_panel.py` entry, `settings.py` (panel-config load + registry resolution with pointer-discovery tier), `panel_log.py`, `app.py` (Tk root + `root.after(100)` dispatcher queue + WM_DELETE placeholder), `views/` window shell (toolbar / master–detail / status bar; both empty states). The panel launches, resolves the registry via the central pointer, and logs to `panel.log`.
- [✓] **Phase B (Discovery, model & daemon list)** — **MILESTONE: the panel is a truthful read-only monitor.** `discovery.py` (registry load, classification matrix, three-way identity, pair resolution, poll thread), `model.py` (observable `AppModel` with pins/deadlines/reconciliation), `views/rendering.py` (display-free status/uptime/summary rules), live `Treeview` daemon list with in-place row updates, and the poller wired through the dispatcher queue in `app.py`.
- [✓] **Phase C (Lifecycle actions)** — **MILESTONE: the panel can start and stop daemons.** `lifecycle.py` (launcher shell-out with temp-file capture, observational drain state machine, kill-time-guarded pair termination, self-owned lock cleanup, `LifecycleController` with transitional pins + busy guard), `views/rendering.py` `action_enablement` (COMP-1 matrix as a pure function), Start/Stop wired in `main_window.py`, controller composed in `app.py` sharing one process table with the poller.
- [✓] **Phase D (Shutdown orchestration)** — **MILESTONE: Core Logic Implementation complete; close = drain-then-terminate all (AC-7/AC-9).** `ShutdownOrchestrator` in `lifecycle.py` (targets = RUNNING + adopted in-flight drains; observational completion via model subscription; watchdog backstop at grace + 15 s), `views/shutdown_dialog.py` (modal Screen-4 dialog, not user-closable, rows + "k of n stopped" from display-free COMP-5 rules in `rendering.py`), `app.py` close path (deferred idempotent exit; zero-target close exits without a dialog; poller runs until actual exit).
- [✓] **Phase E (UI Implementation, PF-TSK-052)** — **MILESTONE: all three detail panes are live.** `log_tail.py` (64 KB window, appended-bytes-only polls with a per-tick cap, rotation reattach, 1,000-line buffer, CRLF-safe), `validation.py` (`--validate` subprocess mirroring the validate launcher; report-file count extraction — TDD §8 Q3 resolved; `ValidationController` with per-project busy guard), `config_edit.py` (safe_load → temp file → real `LinkWatcherConfig.from_file()`+`validate()` → atomic `os.replace`; commented default skeleton), COMP-3/4/6/7 rules in `views/rendering.py`, three pane views (`log_pane.py`, `config_pane.py`, `validation_pane.py`) composed into the notebook with the unsaved-changes guard (row + tab switches), Ctrl+1/2/3, Ctrl+S, Ctrl+End, double-click-row → Log tab.
- [✓] **Phase F (Integration & Testing, PF-TSK-053)** — **MILESTONE: the panel ships and auto-opens.** `single_instance.py` (loopback ephemeral bind + `panel.port` + `SURFACE`/banner handshake + self-owned release), `app.surface()` (deiconify/lift/focus + `ctypes SetForegroundWindow` assist) routed through the dispatcher queue, entry-script wiring that exits a losing launch before any widget is built, hook-wrapper detached `pythonw.exe` auto-open, and installer shipping of `control_panel.py` + panel wrappers + a GUI-safe smoke test.
- [✓] **Phase G (Integration & Testing, PF-TSK-053)** — **MILESTONE: hardening complete; every test-spec scenario and every FDD edge case is covered.** EC-1…EC-11 swept and recorded as a traceability table in the test spec (two gaps found and closed: the EC-7 pipeline half, and COMP-8); INT-8 worker-error isolation across all four worker surfaces plus the UI-thread dispatcher; COMP-2 and COMP-8; the `Alt+S`/`Alt+T`/`F5` toolbar accelerators wired and verified against a real window; the hook wrapper's panel auto-open moved onto every exit path;.
- [✓] Tests: **249/249** passing across ten panel test files (TE-TST-141…144, 146…151). No regressions (**1221** unit tests pass, 3 skipped, 4 xfailed); black/isort/flake8 clean; `Validate-TestTracking.ps1` 0 errors. `views/rendering.py` at **100%** statement coverage; the non-widget panel modules average **~90%** (widget modules and `app.py` are covered by scripted real-Tk runs by design — the test spec forbids widget instantiation).
- [✓] Verified against reality, not only fixtures (2026-08-10): Phase C — panel-start of TimeTrackingV2 (stale-lock reclaim) + panel-stop (clean drain, lock cleanup); Phase D — close-with-running-daemon against a filtered one-project registry: real dialog ("▲ Draining… (0–3s)", "0 of 1 stopped"), clean drain at the idle threshold, lock removed, **app exited by itself 4.9 s after close**; full sequence in `panel.log`. Incidentally confirmed the panel is agnostic to the central registry's key scheme — a parallel session re-keyed PRJ-NNN → APP-NNN mid-session and discovery/lifecycle worked unchanged (keys are opaque `project_id`s). Phase E — scripted live run of the real panel (real Tk, real 500 ms tail tick, real `--validate` subprocess) against a synthetic registered project: **14/14 checks** — log tail rendered + followed a live append, config loaded → dirty → saved byte-exact ("Saved."), invalid value blocked by the real validator with disk untouched, validation found the planted broken link (`✕ 1 broken link`, report at the parsed path). Phase F — the real hook wrapper run three times against a scratch install: returned in **2.4 s with stdout captured through a pipe** (the session-start hang this wrapper exists to prevent did not recur), panel launched under `pythonw.exe` with correctly quoted args and the derived `--project-root`, `panel.port` published, and a repeat launch produced **the same PIDs, the same port, and a `single_instance surface_requested` log line** rather than a second window (AC-11). Installer verified against a clean scratch dir: copy → generated `requirements.txt` → venv created and verified → four wrappers → both `--help` smoke tests green.

### What's In Progress

- [⚙] Nothing in progress — implementation and close-out are complete. **Blocking the next gate**: the feature is entirely untracked in git and must go through Git Commit and Push (PF-TSK-082) before Code Review (PF-TSK-005) has a diff to review (§11 item 3). Then Performance & E2E Test Scoping (PF-TSK-086), then User Documentation Creation (PF-TSK-081)

### What's Blocked

- [🚫] None — all dependency features (0.1.1, 0.1.3, 3.1.1, 6.1.1) are 🟢 Completed; no new packages required

---

## 3. Implementation Progress

### Task Sequence

> Phases map to the [Implementation Plan (PD-IMP-003)](../../technical/implementation-plans/7-1-1-linkwatcher-control-panel-implementation-plan.md) roadmap (TDD §6.2 seven-step sequence). This feature has **no data layer and no database**, so the engine phases run under Core Logic Implementation, the detail panes under UI Implementation, and wiring/hardening under Integration & Testing.

- [✓] **PF-TSK-044**: Feature Implementation Planning

  - **Completed**: 2026-07-15
  - **Key Outputs**: Implementation Plan (PD-IMP-003); this feature state file initialized
  - **Session Notes**: All design inputs were implementation-ready; adopted the TDD §6.2 sequence as the phase skeleton rather than inventing a competing one

- [✓] **PF-TSK-078**: Core Logic Implementation — Phases A–D (COMPLETED 2026-08-10)

  - **Dependencies**: None (all runtime deps 🟢 Completed)
  - **Scope**: Phase A (foundation & settings), Phase B (discovery + model + daemon list → truthful read-only monitor), Phase C (lifecycle: start/drain/terminate + termination guard + lock cleanup), Phase D (shutdown orchestration)
  - **Progress**:
    - ✅ **Phase A (2026-07-27)** — `control_panel.py` entry, `app.py` (Tk root + `root.after(100)` dispatcher queue + WM_DELETE placeholder), `settings.py` (panel-config load + registry resolution **with pointer-discovery tier**), `panel_log.py`, `views/` window shell (toolbar / master–detail / status bar; both empty states). Tests: TE-TST-141 (UNIT-S1…S5 + pointer tier), 13/13 pass. Deviations recorded in §9 (Decisions 2–3).
    - ✅ **Phase B (2026-07-31)** — `discovery.py` poll thread (classification matrix + three-way identity + pair resolution + defensive registry/lock parsing), `model.py` (`AppModel`, pins with deadlines, reconciliation rule), `views/rendering.py` (display-free rendering rules), live `Treeview` list with in-place row updates, poller wired through the dispatcher queue, 1 s UI tick for pin expiry + uptime. Fixtures: local `conftest.py` with the fake process table, fake clock, and lock/registry builders. Tests: TE-TST-142 (25), TE-TST-143 (22), TE-TST-144 (6) — UNIT-D1…D10, UNIT-M1…M7, INT-1/INT-2, all passing. **Milestone reached: truthful read-only monitor.** Deviations recorded in §9 (Decisions 4–6); TDD PD-TDD-033 amended to match (v1.1).
    - ✅ **Phase C (2026-08-10)** — `lifecycle.py` (launcher shell-out via temp-file capture + bounded wait, drain state machine over the newest-log signature, kill-time-guarded pair termination, self-owned lock cleanup, `LifecycleController` with TDD-specified pin TTLs and a one-action-per-project busy guard), `action_enablement` in `views/rendering.py`, Start/Stop wired in `main_window.py` + `app.py`. Fixtures added: fake runner, linked fake terminator, controllable log-stat source, launcher-stub writer. Tests: TE-TST-146 (25 fns — UNIT-L1…L11 + controller), TE-TST-147 (10 cases — COMP-1, created early since the enablement matrix became live code), INT-3/INT-4/INT-9 added to TE-TST-144. **Milestone reached: the panel starts and stops daemons.** Verified live against TimeTrackingV2 (stale-lock reclaim start → clean drain stop → lock cleaned). Deviations recorded in §9 (Decision 7); TDD §9 handoff updated.
    - ✅ **Phase D (2026-08-10)** — `ShutdownOrchestrator` (Tk-free; targets = RUNNING + adopted in-flight drains; observational completion — the poller runs through shutdown so clean drains resolve; `watchdog_fired` backstop at grace + 15 s), `views/shutdown_dialog.py` (modal, not user-closable, renders from the model), COMP-5 rules in `rendering.py` (`shutdown_row_display`, `shutdown_summary` sharing `STILL_DRAINING_STATUSES` with the orchestrator), `app.py` close path with deferred idempotent exit. Tests: INT-5/INT-6 + watchdog + zero-target + adopted-drain in TE-TST-144, five COMP-5 cases in TE-TST-147 (123/123 panel, 1087 total). **Milestone: Core Logic Implementation complete.** Verified live: close-with-running-daemon drained TimeTrackingV2 and exited itself in 4.9 s. Deviations: none; Decision 8 records the observational-exit design.

- [✓] **PF-TSK-052**: UI Implementation — Phase E (COMPLETED 2026-08-10)

  - **Dependencies**: Phase B (model + selection)
  - **Scope**: Detail panes — `log_tail.py` (incremental, rotation-aware), `validation.py` (`--validate` subprocess), `config_edit.py` (validate-on-save + atomic replace), and their `views/` tabs
  - **Progress**:
    - ✅ **Phase E (2026-08-10)** — `log_tail.py` (initial 64 KB window, appended-bytes-only polls capped at 64 KB/tick, rotation/truncation/newer-sibling reattach via the drain watcher's newest-log rule, 1,000-line buffer, CRLF normalization with held-back `\r`), `validation.py` (command builder mirroring `run_linkwatcher_validate.ps1`, Q3-resolved result parser, `ValidationController` — busy guard, worker thread, temp-file capture, 600 s timeout → failed), `config_edit.py` (read states, safe_load → same-dir temp → real `LinkWatcherConfig.from_file()`+`validate()` → `os.replace`, default skeleton), rendering rules for COMP-3/4/6/7, pane views `views/log_pane.py`/`config_pane.py`/`validation_pane.py`, `main_window.py` composition (guard interception on row+tab switches, Ctrl+1/2/3, Ctrl+S, Ctrl+End, double-click → Log), `ValidationController` wired in `app.py`. Tests: TE-TST-148 (UNIT-T1…T5 + read-cap; **corrected 2026-08-14 by Test Audit TE-TAR-094 — CRLF was listed here but is not covered by any test**, see TD264), TE-TST-149 (UNIT-V1…V5 + spawn failure), TE-TST-150 (UNIT-C1…C6 + empty-config contract), INT-7 in TE-TST-144, COMP-3/4/6/7 in TE-TST-147 — panel suite 183/183, 1147 unit tests green. **Milestone: all three detail panes live.** Verified live via scripted real-Tk run against a synthetic project (14/14 checks incl. a real `--validate` subprocess). Deviations recorded in §9 (Decision 9); TDD §8 Q3 resolved, TDD v1.2.

- [✓] **PF-TSK-053**: Integration & Testing — Phase F ✅ (2026-08-10), Phase G ✅ (2026-08-10)

  - **Dependencies**: Phase A (F); Phases B–F (G)
  - **Scope**: Phase F (single instance + hook auto-open + installer changes), Phase G (EC-1…EC-11 sweep, worker error isolation, remaining component tests)
  - **Progress**:
    - ✅ **Phase F (2026-08-10)** — `single_instance.py`: `127.0.0.1` ephemeral bind, `panel.port` published atomically (same-dir temp + `os.replace`), a `SURFACE\n` → `LINKWATCHER-PANEL OK\n` handshake, a daemon listener thread that fires the surface callback through the app's dispatcher queue, fail-open on bind failure, and self-owned release. `app.surface()` (deiconify + lift + focus + `ctypes` `SetForegroundWindow` assist, all best-effort). `control_panel.py` acquires the slot **before** building any widget, so a losing launch exits without a window. Hook wrapper gained a detached `pythonw.exe` auto-open (no `-RedirectStandard*` — that path uses ShellExecute and cannot inherit the hook's capture pipe) with a duplicated install resolver. `install_global.py` ships `control_panel.py`, writes `linkwatcher-panel.bat`/`.ps1`, and smoke-tests the panel via `--help`. Tests: TE-TST-151 (23 functions / 29 cases — UNIT-I1…I4 + recycled-port, raising-callback and release-ownership guards); panel suite 212/212, 1180 unit tests green. **Milestone: the panel ships and auto-opens.** Deviations recorded in §9 (Decision 10); three deployment defects found by Step 16 and fixed (§10).
    - ✅ **Phase G (2026-08-10)** — hardening. **EC sweep**: all eleven FDD edge cases traced to the tests that exercise them (new [Edge-Case Coverage table](../../../test/specifications/feature-specs/test-spec-7-1-1-linkwatcher-control-panel.md#edge-case-coverage-ec-1ec-11) in TE-TSP-045 v1.3); two halves were uncovered and are now closed — EC-7's "the list stays truthful" half (a START_FAILED outcome pin must decay to poll truth) and EC-8's rendering half (COMP-8). **INT-8**: worker-error isolation asserted on all four worker surfaces (start, stop, validation, discovery) *and* the UI-thread dispatcher, each against a real `panel.log` file plus a neighbouring project's row; the dispatcher drain was extracted to a module-level `drain_queue` to make it testable without Tk (§9 Decision 11a). **COMP-2/COMP-8** added, plus a coverage-review gap-fill for `format_uptime`/`summarize` (live since Phase B, never tested) — `views/rendering.py` now at 100%. **Accelerators**: `Alt+S`/`Alt+T`/`F5` wired, each gated on its button's enablement so the COMP-1 matrix governs the keyboard too. **Hook wrapper**: the panel auto-open now runs on every exit path (§10 limitation resolved). **Defect found and fixed**: the list pane claimed "No registered projects found" during the first poll — measured at ~7.5 s on a real machine (§9 Decision 11b). Tests: TE-TST-144 (22 functions), TE-TST-147 (43) — panel suite 249/249, 1221 unit tests green.

- [✓] **PF-TSK-055**: Implementation Finalization — close-out (COMPLETED 2026-08-13)

  - **Readiness**: no open blocking issues — all 4 logged defects fixed and verified in-session; the 4 Known Limitations are checkpoint-accepted (3 Low + 1 resolved); both Open Questions resolved.
  - **Final validation**: full unit suite **1221 passed, 3 skipped, 4 xfailed** (exit 0); panel suite **249/249**; flake8 under the enforced commit gate (`--max-line-length=120 --extend-ignore=E203`) **0 findings**; black + isort clean (31 files); `Validate-TestTracking.ps1` **0 errors**.
  - **Documentation finalized**: [PD-ARC-001 source-code-layout.md](../../technical/architecture/source-code-layout.md) → **v1.2** — the feature had invalidated it (it claimed only `parsers/` and `config/` group modules, and its File Placement tree said "only two subdirectory groupings" with no branch for panel modules). Added a **Panel Subsystem** subsection, an import-rules row, and a panel branch in the decision tree, after verifying the boundary in code: the panel imports `config/` (relative import, for `LinkWatcherConfig` validate-on-save) and **nothing else** from the core, and **no core module imports the panel** — the daemon behaves identically whether or not the panel is installed. Implementation Plan PD-IMP-003 → `status: Completed` (v1.2). §4 reconciled with the real post-Phase-G doc versions. **User documentation deliberately not written here** (human partner's call): the three `❌ Needed` rows in §4 stay flagged for User Documentation Creation (PF-TSK-081), and no README entry was added, to avoid a half-entry that PF-TSK-081 would have to reconcile.
  - **State transition**: feature-tracking → 👀 Needs Review via `Update-FeatureImplementationState.ps1` (the canonical script per the Feature Tracking Mutation Guide — never hand-edited). **The script exited 1 on a defect of its own** (see §10 follow-ups); the feature-tracking write completes before the fault, and the one step it skipped — `Update-WorkflowTracking.ps1` — was run separately and reported *"No workflow status changes needed"*, so WF-010 correctly stays `Pending: 7.1.1` until 🟢 Completed. Verified on disk: nothing was lost.
  - **Two framework-path script defects found and recorded, not fixed** (they route through Support Tasks, not this product task): `New-SourceStructure.ps1 -Update` (CRLF) and `Update-FeatureImplementationState.ps1` (missing `👀 Needs Review` branch). Both in §10 follow-ups.

- [ ] **PF-TSK-005**: Code Review — quality gate (then Performance & E2E Test Scoping, PF-TSK-086). **Prerequisite: commit the feature first — §11 item 3**

---

## 4. Documentation Inventory

### Design Documentation

| Document   | Type        | Status   | Location | Last Updated |
| ---------- | ----------- | -------- | -------- | ------------ |
| [PD-FDD-034](/doc/functional-design/fdds/fdd-7-1-1-linkwatcher-control-panel.md) | FDD | ✅ Created (v1.0) | doc/functional-design/fdds/fdd-7-1-1-linkwatcher-control-panel.md | 2026-06-29 |
| [PD-UIX-003](/doc/technical/design/ui-ux/features/ui-design-7-1-1-linkwatcher-control-panel.md) | UI Design | ✅ Created (v1.1) | doc/technical/design/ui-ux/features/ui-design-7-1-1-linkwatcher-control-panel.md | 2026-08-13 |
| [PD-TDD-033](/doc/technical/architecture/design-docs/tdd/tdd-7-1-1-linkwatcher-control-panel-t2.md) | TDD | ✅ Created (v1.5) | doc/technical/architecture/design-docs/tdd/tdd-7-1-1-linkwatcher-control-panel-t2.md | 2026-08-12 |
| [TE-TSP-045](/test/specifications/feature-specs/test-spec-7-1-1-linkwatcher-control-panel.md) | Test Specification | ✅ Created (v1.4) | test/specifications/feature-specs/test-spec-7-1-1-linkwatcher-control-panel.md | 2026-08-12 |
| [PD-IMP-003](/doc/technical/implementation-plans/7-1-1-linkwatcher-control-panel-implementation-plan.md) | Implementation Plan | ✅ Created (v1.2) | doc/technical/implementation-plans/7-1-1-linkwatcher-control-panel-implementation-plan.md | 2026-08-13 |
| [PD-ARC-001](/doc/technical/architecture/source-code-layout.md) | Architecture (updated) | ✅ Updated (v1.2) | doc/technical/architecture/source-code-layout.md | 2026-08-13 |

> **Post-Phase-G design-doc amendments** (recorded at Implementation Finalization, 2026-08-13, since they landed after the Phase G narrative was written): TDD → **v1.5** (2026-08-12; D-T1 confirmed — the PySide6 fallback is withdrawn, the model/view split retained because it is what makes the view rules testable without a display); Test Spec → **v1.4** (2026-08-12); UI Design → **v1.1** (2026-08-13; re-approved by the human partner after screen-reader support was removed from scope in §5.1/§5.2 and the per-component specs — consistent with the UX row in §7). Implementation Plan set to `status: Completed` and PD-ARC-001 updated at this task (see §3, PF-TSK-055).

### User Documentation

> See [Diátaxis Content Type Guide (PF-GDE-063)](../../../process-framework/guides/07-deployment/diataxis-content-type-guide.md) for the decision matrix, content-type values, and status taxonomy (`✅ Created` / `✅ Covered Elsewhere` / `❌ Needed` / `N/A`).

| Document   | Content Type | Status   | Location | Last Updated |
| ---------- | ------------ | -------- | -------- | ------------ |
| Control Panel operations | how-to | ❌ Needed | (to be created) | — |
| Control Panel settings & status reference | reference | ❌ Needed | (to be created) | — |
| Panel shutdown / close-terminates-all model | explanation | ❌ Needed | (to be created) | — |
| — | tutorials | N/A | — | Panel auto-opens and is a discoverable master–detail window; no zero-to-first-success walkthrough needed |

> **User-visible feature.** Applying the [Diátaxis decision matrix](../../../process-framework/guides/07-deployment/diataxis-content-type-guide.md#decision-matrix): the panel introduces a new user workflow (how-to), has settings/status facts to look up — `panel-config.yaml` keys, registry resolution, status meanings (reference), and a non-obvious close-terminates-all + forced-stop model that benefits from explanation. Each `❌ Needed` row independently triggers [User Documentation Creation (PF-TSK-081)](../../../process-framework/tasks/07-deployment/user-documentation-creation.md) **after** Performance & E2E Test Scoping. Rows are biased toward inclusion; PF-TSK-081 prunes at creation time.

### Developer Documentation

_N/A during planning — no developer/API reference documentation for this feature (no new API contract per PD-FDD-034). Revisit if implementation surfaces reusable developer-facing seams._

### Existing Project Documentation

> Records pre-existing project documentation identified during onboarding audit (PF-TSK-064 step 3b). Content relevance is confirmed during analysis (PF-TSK-065). Confirmed entries guide documentation creation (PF-TSK-066) to extract rather than re-derive.
>
> For new projects: _No pre-existing project documentation identified._

_Not applicable — this is a new (greenfield) feature, not an onboarding/retrospective one. No pre-existing project documentation._

### Quick Links

- **Main Design**: [TDD PD-TDD-033](../../technical/architecture/design-docs/tdd/tdd-7-1-1-linkwatcher-control-panel-t2.md) · [FDD PD-FDD-034](../../functional-design/fdds/fdd-7-1-1-linkwatcher-control-panel.md) · [UI Design PD-UIX-003](../../technical/design/ui-ux/features/ui-design-7-1-1-linkwatcher-control-panel.md)
- **Implementation Plan**: [PD-IMP-003](../../technical/implementation-plans/7-1-1-linkwatcher-control-panel-implementation-plan.md)
- **Related Features**: 0.1.1 Core Architecture, 0.1.3 Configuration, 3.1.1 Logging, 6.1.1 Link Validation

---

## 5. Code Inventory

### File Inventory

> All rows below are **implemented** (Phases A–G complete; every row carries its ✅ date). Origin reflects the operation performed. Design-doc sections informing each file: TDD §4.x as noted.

| File Path | Role | Ownership | Origin | Notes |
| --------- | ---- | --------- | ------ | ----- |
| `control_panel.py` (repo root) | Core | Owned | Created | ✅ Created 2026-07-27. Thin entry beside `main.py` (mirrors flat install layout); adds `--project-root` for pointer discovery. TDD §4.0 |
| `src/linkwatcher/linkwatcher_control_panel/__init__.py` | Core | Owned | Modified | ✅ Populated 2026-07-27 — re-exports settings + panel_log (GUI-free imports) |
| `src/linkwatcher/linkwatcher_control_panel/app.py` | Core | Owned | Created | ✅ Phases A–F (2026-07-27 … 2026-08-10) — Tk root, dispatcher queue (`root.after(100)`), model, discovery poller, 1 s UI tick, Refresh, `LifecycleController` (shared process table), the Phase D close path (WM_DELETE → `ShutdownOrchestrator` + `ShutdownDialog` + watchdog → deferred idempotent exit), the Phase E `ValidationController` wiring, and the Phase F `surface()` raise/focus entry point. Phase G extracted the dispatcher drain into a module-level `drain_queue()` (Tk-free, so its isolation rule is testable). TDD §4.0/§4.3/§4.4 |
| `src/linkwatcher/linkwatcher_control_panel/settings.py` | Core | Owned | Created | ✅ Created 2026-07-27. panel-config load + registry resolution chain **with pointer-discovery tier** (§9 Decision 2). TDD §4.1 |
| `src/linkwatcher/linkwatcher_control_panel/panel_log.py` | Supporting | Owned | Created | ✅ Created 2026-07-27. Shared panel diagnostic log (`logs/panel.log`); module beyond TDD §9 list (§9 Decision 3) — avoids an app.py import cycle |
| `src/linkwatcher/linkwatcher_control_panel/model.py` | Core | Owned | Created | ✅ Created 2026-07-31 (grows B→E). Observable `AppModel`, pins with deadlines, reconciliation rule, notification-on-change contract. TDD §4.1/§4.3 |
| `src/linkwatcher/linkwatcher_control_panel/discovery.py` | Core | Owned | Created | ✅ Created 2026-07-31. Poll thread, classification matrix, normalized three-way identity, pair resolution, defensive registry/lock parsing. `ProcessTable` seam (`PsutilProcessTable` real / fake in tests). TDD §4.4 |
| `src/linkwatcher/linkwatcher_control_panel/lifecycle.py` | Core | Owned | Created | ✅ Created 2026-08-10 (Phase C); ✅ Phase D — launcher shell-out (temp-file capture — pipes hang on inherited handles), drain state machine, kill-time termination guard, self-owned lock cleanup, `LifecycleController` (pins + busy guard), and `ShutdownOrchestrator` (close-time fan-out, observational completion, watchdog). TDD §4.4 |
| `src/linkwatcher/linkwatcher_control_panel/log_tail.py` | Supporting | Owned | Created | ✅ Created 2026-08-10 (Phase E). Incremental rotation-aware tail: 64 KB window, appended-bytes polls (64 KB/tick cap), newest-log reattach, 1,000-line buffer, CRLF-safe incremental decode. TDD §4.5 |
| `src/linkwatcher/linkwatcher_control_panel/validation.py` | Supporting | Owned | Created | ✅ Created 2026-08-10 (Phase E). `--validate` subprocess (mirrors the validate launcher), exit-code→state, Q3-resolved count/report extraction, `ValidationController` (busy guard + worker + 600 s bound). TDD §4.6 |
| `src/linkwatcher/linkwatcher_control_panel/config_edit.py` | Supporting | Owned | Created | ✅ Created 2026-08-10 (Phase E). Read states (EC-10), safe_load → same-dir temp → real `LinkWatcherConfig.from_file()`+`validate()` → atomic `os.replace`; commented default skeleton. TDD §4.6 |
| `src/linkwatcher/linkwatcher_control_panel/single_instance.py` | Supporting | Owned | Created | ✅ Created 2026-08-10 (Phase F). Loopback ephemeral bind + atomically published `panel.port` + `SURFACE`/banner handshake; listener thread, fail-open bind, self-owned release. TDD §4.5 |
| `src/linkwatcher/linkwatcher_control_panel/views/` | Core | Owned | Created | ✅ Phases A–E — `main_window.py` (live `Treeview`, Start/Stop enablement; Phase E: pane composition, unsaved-guard interception on row+tab switches, Ctrl+1/2/3 / Ctrl+S / double-click→Log), `rendering.py` (display-free rules: COMP-1, COMP-5; Phase E: COMP-3 `validation_display`, COMP-4 `config_notice`, COMP-6 `unsaved_guard_outcome`, COMP-7 `ScrollLock`), `shutdown_dialog.py` (Phase D), and the Phase E panes `log_pane.py` / `config_pane.py` / `validation_pane.py` (three modules inside the TDD-listed `views/` package, per the Decision 6 precedent). Phase G: COMP-2/COMP-8 rules — `list_pane_display` (tree / loading / empty / error) — plus the `Alt+S`/`Alt+T`/`F5` accelerators, each gated on its button's enablement. TDD §4.2, UI Design PD-UIX-003 |
| `process-framework/tools/linkWatcher/start_linkwatcher_hook_wrapper.ps1` | Supporting | Co-owned | Modified | 🔴 **NOT PRESENT — see §10 CR-1 (Code Review, 2026-08-17).** The file carries no `pythonw` / `control_panel` / `Open-ControlPanel` reference in the working tree, at HEAD, or anywhere in git history. The change described below was made and live-verified on 2026-08-10 and then silently reverted by the 2026-08-14 framework rollout; it was never committed because the feature was untracked until 2026-08-17. FR-8 / UI-7 / BR-8 / AC-11 first clause are **undelivered**. Re-implementation routes to Process Improvement (framework path). *Original entry, retained for the record:* ✅ Modified 2026-08-10 (Phase F). Detached `pythonw.exe` auto-open (no redirection → ShellExecute → no inherited capture pipe), duplicated install resolver, derived `--project-root`, never throws/waits. Phase G: auto-open extracted into `Open-ControlPanel` and invoked on **all three** exit paths (both functions now declared above the early-exit branches — PowerShell does not hoist script-level function definitions). TDD §4.5 |
| `deployment/install_global.py` | Supporting | Co-owned | Modified | ✅ Modified 2026-08-10 (Phase F). Ships `control_panel.py`; adds `linkwatcher-panel.bat`/`.ps1`; GUI-safe `--help` smoke test; generates `requirements.txt` from pyproject runtime deps (fixes the unbuildable flat-tree `pip install`); venv verification now asserts every declared runtime dep. TDD §9 |
| `pyproject.toml` | Supporting | Co-owned | Modified | ✅ Modified 2026-08-10 (Phase F). `psutil` moved from the `[test]` extra to runtime `dependencies`; `GitPython` added — both were real runtime imports missing from the installed venv (§10) |

- **Role**: Core (central to feature), Supporting (used but not central), Shared (used by multiple features)
- **Ownership**: Owned (this feature is primary owner), Co-owned (shared responsibility), External (owned by another feature), Tech Debt (file is owned but slated for archival/removal — explain why in Notes; common subtypes: `.bak` stub, Dead Code, Stale Doc)
- **Origin**: Created (new file), Modified (changed existing file), Pre-existing (used without changes)

**Code Markers**: Created files include `// FEATURE: {feature-id}` header marker. Modifications include `// [FEATURE: {feature-id}]` inline markers.

> **Do not include LOC counts, method counts, or other size metrics** in file descriptions. These are point-in-time snapshots that go stale as code evolves and add maintenance burden without clear value. Use `wc -l` or `grep` to check current sizes on demand.

### Test Files

> Planned per [Test Spec TE-TSP-045](../../../test/specifications/feature-specs/test-spec-7-1-1-linkwatcher-control-panel.md), created via `New-TestFile.ps1` into `test/automated/unit/7-operations-control/7-1-control-panel/`. TE-TST IDs assigned at creation.

| Test File | Type | Coverage Areas | Status   | Created    |
| --------- | ---- | -------------- | -------- | ---------- |
| `test_panel_settings.py` | Unit | UNIT-S1…S5 + pointer tier (settings + registry resolution) | ✅ Created (TE-TST-141), 13/13 pass | 2026-07-27 |
| `test_panel_discovery.py` | Unit | UNIT-D1…D10 (classification, three-way identity, pair) + identity guards (case/separator, sibling-prefix, unattributable process) | ✅ Created (TE-TST-142), 25 functions / 33 cases pass | 2026-07-31 |
| `test_panel_model.py` | Unit | UNIT-M1…M7 (observable, pins, expiry, selection) | ✅ Created (TE-TST-143), 22 functions / 23 cases pass | 2026-07-31 |
| `test_panel_lifecycle.py` | Unit | UNIT-L1…L11 + controller orchestration + busy guard (start, drain machine, termination guard, lock cleanup) | ✅ Created (TE-TST-146), 25 functions / 25 cases pass | 2026-08-10 |
| `test_panel_log_tail.py` | Unit | UNIT-T1…T5 + per-tick read cap (incremental tail, rotation, bounded buffer). **CRLF normalization is NOT covered** — corrected 2026-08-14 by Test Audit (TE-TAR-094), tracked as TD264 | ✅ Created (TE-TST-148), 13 functions pass | 2026-08-14 |
| `test_panel_validation.py` | Unit | UNIT-V1…V5 + spawn failure (exit→state, count/report, busy guard, command construction) | ✅ Created (TE-TST-149), 15 functions pass | 2026-08-10 |
| `test_panel_config_edit.py` | Unit | UNIT-C1…C6 + empty-config contract (validate-on-save, atomic/interrupted save, skeleton) | ✅ Created (TE-TST-150), 15 functions pass | 2026-08-10 |
| `test_panel_single_instance.py` | Unit | UNIT-I1…I4 (socket bind, SURFACE, stale port) + recycled-port rejection, raising-callback isolation, release ownership | ✅ Created (TE-TST-151), 23 functions / 29 cases pass | 2026-08-10 |
| `test_panel_integration.py` | Integration | INT-1…INT-9 (poller→model pipeline, parallel drains, bounded exit, error isolation) + the EC-7 pipeline half | ✅ Complete (TE-TST-144) — 22 functions pass; Phase G added INT-8 (four worker surfaces + the UI-thread dispatcher, asserted against a real panel.log) and the EC-7 outcome-pin decay | 2026-08-10 |
| `test_panel_views.py` | Component | COMP-1…COMP-8 (enablement, status rendering, single-visible-state, empty/wait state) | ✅ Complete (TE-TST-147) — 43 functions/cases pass; Phase G added COMP-2, COMP-8, the uptime/summary gap-fill and the pending-discovery wait state | 2026-08-10 |
| `conftest.py` (local) | Fixtures | Fake process table, fake clock, log-stat source, runner/terminator fakes, lock/registry/launcher builders | ✅ Created 2026-07-31; extended Phase C 2026-08-10 — added `FakeRunner` (scriptable launcher results), `FakeTerminator` (linked to the process table), `FakeLogStat` (controllable drain driver), `write_launcher` | 2026-08-10 |

### Database/Schema Changes

_N/A — no database, no schema, no migrations. The only persisted panel state is the optional `panel-config.yaml` settings file and the transient `panel.port` single-instance file._

---

## 6. Dependencies

### Feature Dependencies

**This Feature Depends On**:

- **0.1.1 Core Architecture** — 🟢 Completed
  - Why: `main.py` CLI entry + `.linkwatcher.lock` per-project PID lock (daemon identity + start path)
  - Impact if unavailable: no identity resolution or start path — feature not buildable
- **0.1.3 Configuration System** — 🟢 Completed
  - Why: `LinkWatcherConfig.from_file()/validate()` reused for config validate-on-save (D-T6, no parallel validator)
  - Impact if unavailable: config editor cannot validate before save
- **3.1.1 Logging & Monitoring** — 🟢 Completed
  - Why: per-project `logs/linkwatcher/LinkWatcherLog*.txt` — log pane tail + drain quiescence heuristic
  - Impact if unavailable: no log view; drain heuristic has nothing to observe
- **6.1.1 Link Validation** — 🟢 Completed
  - Why: `--validate` scan the Validation pane runs as a short-lived subprocess (no lock contention)
  - Impact if unavailable: Validation pane inert

**Other Features Depend On This**:

- None currently. The panel is a leaf supervisor; no product feature depends on it.

### System Dependencies

**Required Services**:

- Central `project-registry.json` (in appdev central) — read-only source of the registered-projects list
- The existing `SessionStart` hook / launcher scripts — remain the automatic start path; the panel coexists and (via the hook wrapper) auto-opens

**Required Packages**:

| Package | Version   | Purpose   | Added      |
| ------- | --------- | --------- | ---------- |
| psutil  | >= 5.9.0  | Process enumeration / identity / termination | Already present in `pyproject.toml` |
| Tkinter/ttk | stdlib | GUI toolkit (Decision D-T1) | N/A — Python standard library |

> **Zero new package dependencies.**

### Code Dependencies

**Existing Code This Feature Integrates With**:

| Component | Used For  | Methods/APIs Used | Notes   |
| --------- | --------- | ----------------- | ------- |
| `src/linkwatcher/config` (`LinkWatcherConfig`) | Config validate-on-save | `from_file()`, `validate()` | Never mocked in tests (D-T6) |
| `process-framework/tools/linkWatcher/start_linkwatcher_background.ps1` | Daemon start | Shell-out; exit 0 = started/already-running, exit 1 = failure + stderr | Reuses launcher singleton/stale-lock guards (D-T4) |
| `main.py --validate` | Validation scan | Subprocess; `--config`/`--project-root` args | Takes no lock; mirrors `run_linkwatcher_validate.ps1` |
| `.linkwatcher.lock` + psutil process table | Daemon identity & termination | Lock PID read; cmdline + create_time inspection; terminate | Three-way identity required before any kill (D-T5) |

---

## 7. Dimension Profile

> **📖 Reference**: [Development Dimensions Guide](../../../process-framework/guides/framework/development-dimensions-guide.md)
>
> **Source**: Feature Implementation Planning (PF-TSK-044) | **Last reviewed**: 2026-07-15
>
> All 10 dimensions apply to this feature (none N/A) — core dimensions listed alongside extended for completeness, since AC and the three Critical extended dimensions drive implementation.

### Applicable Dimensions

| Dimension | Importance | Key Considerations |
|-----------|-----------|-------------------|
| Architectural Consistency (AC) | Critical | First GUI surface introduces new patterns (observable model, worker-thread + UI-dispatcher queue); hard rule — Tk touched only from the UI thread, views render from the model one-way |
| Security & Data Protection (SE) | Critical | Three-way identity agreement (lock PID + `main.py` + project root) before any terminate — PIDs get recycled; config never written unvalidated; loopback-only socket; registry/lock treated as untrusted inputs |
| Accessibility / UX Compliance (UX) | Critical | Keyboard-only operability; glyph+text status (never color-only). Screen-reader support is out of scope for this tool |
| Data Integrity (DI) | Critical | Atomic config save (temp + `os.replace`); lock deleted only if it still holds the terminated PID; forced termination must never corrupt files (bounded to dropped in-flight updates, BR-7) |
| Code Quality & Standards (CQ) | Relevant | Explicit per-surface error handling — every external surface fails into a pane-local error state, never a silent swallow or crashed mainloop; small injectable-seam modules |
| Integration & Dependencies (ID) | Relevant | Consumes 4 external surfaces read-only + the central registry; exit-code / lock / config contracts must be honored without parallel reimplementations; no new dependency added |
| Documentation Alignment (DA) | Relevant | Keep this state file and the TDD in sync; record resolutions of the 3 open TDD questions (Tkinter verdict, frozen-project filtering, count extraction) as they land |
| Extensibility & Maintainability (EM) | Relevant | Injectable seams are a test obligation; model/view split keeps the toolkit swappable; thresholds externalized to `panel-config.yaml` |
| Performance & Scalability (PE) | Relevant | UI thread never blocked (all I/O off-thread); incremental log tail into a bounded buffer; single operator / single-digit projects — no scale pressure |
| Observability (OB) | Relevant | Panel log (WARN+) records every forced termination, start failure, validation failure, and caught worker exception — the record for "why was my daemon force-stopped" |

### Not Applicable

| Dimension | Rationale |
|-----------|-----------|
| — | None. All 10 dimensions apply to this feature. |

---

## 8. Quality Assessment

> **Onboarding Quality Gate** (PF-TSK-065): Populated during Codebase Feature Analysis. Scores determine whether this feature is documented as **As-Built** (descriptive) or **Target-State** (prescriptive with gap analysis) during PF-TSK-066.
>
> Dual-score model (PF-IMP-019/032): **Code Maturity** drives the As-Built/Target-State classification; **Test Maturity** drives a separate test-plan-urgency signal (does not affect classification).
>
> For new features (not onboarding): _Leave this section empty — quality gate applies only during framework adoption._

**Classification**: N/A — new (greenfield) feature; the onboarding quality gate applies only during framework adoption.

---

## 9. Design Decisions

> **Architectural/technical decisions (toolkit, grace period, single-instance mechanism, start path, termination identity, config validation reuse) are owned by the TDD** (PD-TDD-033 §9, decisions D-T1…D-T6) and are not restated here. This section records **planning-phase** decisions only.

### Decision 1: Adopt the TDD §6.2 seven-step sequence as the phase skeleton

**Date**: 2026-07-15
**Context**: The planning task must produce a phase sequence and map it to decomposed implementation tasks. The TDD already specified a 7-step implementation order, each step leaving the panel runnable.

**Options Considered**:

1. Reuse the TDD's 7-step order verbatim as 7 plan phases - Pros: no competing sequence, each step demonstrable, test spec's priority order already aligns to it. Cons: none material.
2. Invent a fresh phase decomposition grouped by architectural layer - Pros: matches the generic template. Cons: this feature has no data/state/UI layer split; would fight the design and diverge from the test spec's "implement alongside TDD steps 2–4" guidance.

**Decision Made**: Option 1 — 7 phases (A–G) 1:1 with the TDD steps.

**Rationale**: The TDD sequence is dependency-correct and demonstrable at every step; the test spec's priority order is written against it. A competing decomposition would create needless divergence across three documents.

**Implications**:

- Phases map to tasks by nature of work: engine phases A–D → Core Logic Implementation (PF-TSK-078); detail panes E → UI Implementation (PF-TSK-052); wiring/hardening F–G → Integration & Testing (PF-TSK-053).
- No data-layer or state-management decomposed task is used (no data layer exists).

**Validation**: Each phase ends in a runnable panel with its slice of passing tests; the milestone after Phase B is a truthful read-only monitor.

---

### Decision 2: Add a `.framework-central-pointer` discovery tier to the registry resolution chain

**Date**: 2026-07-27 (Core Logic Implementation, Phase A)
**Context**: TDD §4.1 specified a three-source registry chain (`--registry` → env → `panel-config.yaml` key → error banner). Out of the box — a hook-auto-opened panel with no config — that lands on the error banner, a poor first-run experience for a tool meant to auto-open and "just show your daemons."

**Options Considered**:

1. Keep the TDD's three-source chain - Pros: matches the TDD verbatim. Cons: zero-config launch shows an error banner; forces the operator to configure a path the framework already knows how to find.
2. Add pointer-based discovery as a fourth tier before the error - Pros: reuses the framework's canonical "where is central?" locator (`.framework-central-pointer`) → no second source of truth; the hook (which knows its project) passes `--project-root` and a manual launch from inside a project is found by a cwd walk-up, so both common paths become zero-config. Cons: small deviation from the TDD; the panel reads a framework-infra file (but it already reads the central registry — same layer).
3. Bake the resolved central path into a shipped `panel-config.yaml` at install time - Pros: context-independent at runtime. Cons: records central's location in a *second* place that can drift from the pointer.

**Decision Made**: Option 2 — checkpoint-approved 2026-07-27.

**Rationale**: One source of truth for central's location; zero-config for both the hook-auto-open and manual-from-project paths; the error banner remains only for the genuinely unconfigured case. The three explicit sources still outrank it, so nothing that worked before changes.

**Implications**: TDD §4.1 resolution order updated. The hook wrapper will pass `--project-root` (Phase F); until then the cwd walk-up covers manual launches. Pointer discovery counts only when the derived registry file exists (a phantom path falls through to the error state). Covered by the UNIT-S4 pointer-tier tests.

### Decision 3: Give the panel diagnostic log its own module (`panel_log.py`)

**Date**: 2026-07-27 (Core Logic Implementation, Phase A)
**Context**: The TDD (§3.3) mandates a panel log (`<install>/logs/panel.log`) but its §9 module list does not enumerate a module for it. The log is written by many modules (discovery, lifecycle, validation, config_edit, app).

**Decision Made**: A dedicated `panel_log.py` exposing `setup_panel_log()` / `get_panel_logger()`.

**Rationale**: A shared logger obtained from a dedicated module lets every worker module call `get_panel_logger()` without importing `app.py` (which imports *them*) — avoiding an import cycle. Centralizing the setup keeps it testable and idempotent, and console-less-safe (guards `sys.stderr is None` under `pythonw`).

**Implications**: One module beyond the TDD §9 list (a minor structural addition, not a behavior change).

### Decision 4: Identity matches the normalized `--project-root` *value*, not a command-line substring

**Date**: 2026-07-31 (Core Logic Implementation, Phase B)
**Context**: TDD §4.4 specified the three-way identity test as "command line contains `main.py` **and** the project root" — the same literal-substring rule the launcher's `Test-LinkWatcherAlreadyRunning` uses. Inspecting the actually-running daemons showed the registry stores `C:\Users\...\LinkWatcher` while the live daemon's command line carries `c:\Users\...\LinkWatcher` (lowercase drive letter, supplied by the hook's cwd).

**Options Considered**:

1. Implement the rule literally - Pros: matches the TDD text and the launcher verbatim. Cons: **demonstrably broken today** — LinkWatcher's own running daemon classifies as STALE_LOCK, and Phase C's Stop would refuse to act on it. Also admits a false positive: root `C:\proj\App` substring-matches a daemon for `C:\proj\AppExtra`, i.e. a wrong-process termination target.
2. Extract the `--project-root` argument value and compare it normalized (case-fold + separator normalization) - Pros: fixes both defects; strictly stronger than substring matching; still exactly the three-way agreement D-T5 requires. Cons: small deviation from the TDD wording; a process with no `--project-root` in its command line becomes unidentifiable.
3. Normalize but keep substring containment as a fallback - Pros: tolerant of unusual command lines. Cons: reintroduces the prefix false positive — the one failure mode with the worst consequence.

**Decision Made**: Option 2 — checkpoint-approved 2026-07-31.

**Rationale**: The TDD's *intent* (D-T5: never act on the wrong process) is served by Option 2 and actively defeated by Option 1. Comparing the parsed argument value is both more permissive where it should be (case) and stricter where it must be (prefixes).

**Implications**: A daemon started without `--project-root` (the flag defaults to the working directory, invisible in the command line) is never claimed by any project — the conservative choice, since an unattributable process must never be terminated. TDD §4.4 step 2 and D-T5 updated. Covered by three dedicated guard tests plus the `extract_project_root` parametrization.

### Decision 5: A live daemon outranks a stale lock in classification

**Date**: 2026-07-31 (Core Logic Implementation, Phase B)
**Context**: The TDD §4.4 matrix reads lock-first: "lock exists but PID dead/mismatched → STALE_LOCK". It does not say what to do when a stale lock coexists with a daemon that the command-line scan *does* find — a real combination (a reclaimed or orphaned lock alongside a running daemon).

**Decision Made**: Classify RUNNING, with the stale lock surfaced in the row's detail note. STALE_LOCK is reserved for a lock with genuinely nothing behind it.

**Rationale**: The process table is truth; the lock is diagnostic. Rendering a *running* daemon as not-running would leave it unstoppable from its only supervisor — the exact blindness the feature exists to remove. EC-1/EC-3 are unaffected: a lock with no live daemon is still STALE_LOCK with empty `pids`, so nothing can be acted on.

**Implications**: TDD §4.4 step 4 rewritten as an explicit precedence list. Also resolved there: a registered project whose root is missing from disk is STOPPED with a "project root not found" detail, rather than a new `DaemonStatus` member or a silently dropped row.

### Decision 6: Rendering rules live in a display-free `views/rendering.py`

**Date**: 2026-07-31 (Core Logic Implementation, Phase B)
**Context**: The daemon list needs status glyph+label, uptime formatting, and a summary line. Putting these in the widget code would make COMP-1…COMP-8 (specified as headless, no Tk instantiation) untestable.

**Decision Made**: A pure-function module `views/rendering.py` (`status_display`, `status_text`, `format_uptime`, `summarize`); `main_window.py` only calls into it.

**Rationale**: Discharges the TDD §4.2 "render from model only" obligation structurally rather than by convention, pre-factors the Phase G component tests, and keeps a toolkit swap confined to `views/`. Like `panel_log.py` (Decision 3), it is one module beyond the TDD §9 list but inside the already-listed `views/` package, so no TDD file-list change was needed.

### Decision 7: Termination acts on every kill-time-matching process, not `resolve_pair` from the lock anchor

**Date**: 2026-08-10 (Core Logic Implementation, Phase C)
**Context**: TDD §4.4 says every lifecycle operation "acts on the pair", and the Phase B handoff suggested reusing `resolve_pair` at kill time. Implementing the kill-time guard showed a simpler rule with strictly better properties.

**Options Considered**:

1. Re-anchor on the poll-time lock PID and expand via `resolve_pair` - Pros: matches the handoff note literally. Cons: depends on poll-time data the guard exists to distrust; misses accidental duplicate daemons for the same project (they are outside the anchored pair but equally this project's daemons).
2. Terminate the full set of processes passing `is_daemon_for` at kill time - Pros: the guard *is* the target selection — one rule, fresh snapshot, nothing unidentified ever touched; the verified pair is this set by construction, and accidental duplicates (the PF-IMP-967 condition) are cleaned up too. Cons: none found — the set is always a superset of the pair and every member independently passes three-way identity.

**Decision Made**: Option 2 — presented at the Phase C scope checkpoint (approved 2026-08-10) and recorded here.

**Implications**: `resolve_pair` remains discovery-only (classification/uptime). UNIT-L8/L9 assert the pair-and-nothing-else and refusal properties. Three further implementation choices from the same checkpoint: launcher output captured via **temp files, never pipes** (the spawned daemon inherits handles and a pipe-capturing parent hangs — launcher header / TE-E2E-009); start-failure reason falls back from stderr to the launcher's stdout error lines (`Write-Host` lands on stdout); outcome pins (FORCE_STOPPED / START_FAILED) carry a **30 s TTL** before poll truth wins again.

### Decision 8: Shutdown completion is observational — the poller runs through shutdown

**Date**: 2026-08-10 (Core Logic Implementation, Phase D)
**Context**: The shutdown dialog must truthfully flip rows to "✔ Stopped", and the app must exit exactly when every target is resolved. A clean drain leaves its DRAINING pin for the poll to resolve (Phase C design), so something must still be observing reality during shutdown.

**Options Considered**:

1. Keep the poller running until actual exit; the orchestrator subscribes to the model and completes when every target leaves the still-draining set - Pros: one truth mechanism end to end — the same observation loop that makes the panel truthful also decides the exit; no second completion signal to reconcile. Cons: exit lags the last drain by up to one poll interval (≤ 2.5 s).
2. Have shutdown workers post explicit terminal outcomes and stop the poller at close - Pros: exit fires the instant the last worker returns. Cons: invents a shutdown-only completion channel that bypasses the reconciliation rule; rendered state and exit condition can then disagree with reality (the exact failure mode the model design exists to prevent).

**Decision Made**: Option 1, with a **watchdog backstop** at grace + 15 s (exit-wait 10 s + slack) that forces completion regardless of worker state — checkpoint-approved 2026-08-10.

**Rationale**: AC-9 is served by bounds, not by speed: every drain is internally bounded, the watchdog bounds the pathological case, and the ≤ 2.5 s observation lag is irrelevant next to the 3–20 s drain itself. Truthfulness stays a single mechanism.

**Implications**: `STILL_DRAINING_STATUSES` is defined once in `lifecycle.py` and shared by the orchestrator and the COMP-5 rendering rules, so the dialog and the exit condition can never count "resolved" differently. Rows already DRAINING from a user stop are adopted as targets (busy guard rejects the duplicate dispatch). A STARTING row at close time is **not** drained — recorded as a Known Limitation (§10). Verified live: close-with-running-daemon exited by itself 4.9 s after close.

### Decision 9: Validation count from the report file; config validated via `from_file` on the save temp

**Date**: 2026-08-10 (UI Implementation, Phase E)
**Context**: Two open implementation choices landed at the Phase E checkpoint: how to extract the broken-link count (TDD §8 Q3 — stdout summary vs. report file), and where exactly the config save pipeline should run the real validator.

**Decision Made** (checkpoint-approved 2026-08-10):

1. **Q3**: the report *path* is parsed from stdout's ANSI-stripped `Report written to <path>` line (falling back to the conventional `logs/linkwatcher/LinkWatcherBrokenLinks.txt`); the *count* comes from the report file's stable, colorless `Broken links  : N` line (falling back to the same line in stdout). Exit 1 without a locatable report is `failed`, never a result (EC-6).
2. **Config save**: `yaml.safe_load` first (line-numbered errors + top-level-mapping check), then the candidate bytes are written to a same-directory temp file and constructed via the real `LinkWatcherConfig.from_file()` + `validate()` **on that temp file** — the exact bytes to be persisted go through the exact code path the daemon uses at startup (D-T6 extended to the parse itself) — then `os.replace`. An empty/comments-only config is valid by the documented "empty means defaults" contract.

**Implications / companion details from the same checkpoint**: `LogTail` normalizes CRLF with a held-back `\r` (daemon logs are text-mode `\r\n`; a pair split across reads must not leak); per-tick reads are capped at 64 KB (UI-thread guard); rotation reattach also fires when a *newer* `LinkWatcherLog*.txt` sibling appears while the current file is quiet (strictly stronger than the TDD's "missing or shrank" wording, aligned with the drain watcher's newest-by-mtime rule); scroll-lock is spec-literal (only the checkbox or Ctrl+End resume following); the three pane views live inside the TDD-listed `views/` package (Decision 6 precedent). Toolbar accelerators (Alt+S/Alt+T/F5) from PD-UIX-003 §4.2 were observed unwired (Phase C code) — deferred to Phase G hardening.

### Decision 10: A banner handshake proves the peer is a panel; the simultaneous-launch race is accepted

**Date**: 2026-08-10 (Integration & Testing, Phase F)
**Context**: TDD §4.5 specifies "a second launch reads the port file, connects, sends `SURFACE`, and exits; connect failure ⇒ stale port file ⇒ the new launch takes over." Implementing it surfaced two gaps the wording leaves open.

**(a) A bare successful connect is not proof of a panel.** Ephemeral ports are recycled, so a stale `panel.port` can name a port some unrelated process now owns. Under the literal rule that connect succeeds, the launch reports "already running" and **the panel never opens again** until the file is deleted by hand.

**Options Considered**:

1. Implement literally - Pros: matches the TDD text. Cons: a recycled port permanently blocks the panel — a silent, self-inflicted denial of the whole feature.
2. Require a one-line ack (`SURFACE\n` → `LINKWATCHER-PANEL OK\n`) before believing an incumbent exists - Pros: makes "connect failure ⇒ stale" actually trustworthy, which is what the TDD wanted; still a fixed one-verb protocol with no data surface. Cons: two bytes of protocol beyond the TDD wording.

**Decision Made**: Option 2 — the handshake *is* the stale-detection mechanism the TDD assumed connect alone provided. Covered by a dedicated test that stands up a foreign listener on the published port and asserts the panel still opens.

**(b) Simultaneous launches can both win.** Read → connect-fail → bind → publish is not atomic, so two launches within the same few milliseconds both fail to connect, both bind different ephemeral ports, and both publish; the later writer owns the file and the other panel becomes unreachable (it stabilizes at exactly two windows, never more). **Accepted as a Known Limitation** (checkpoint-approved 2026-08-10) rather than adding a second exclusion primitive: the window is sub-millisecond, both windows are visible to the operator, and EC-9's actual harm — conflicting start/stop — requires the operator to drive both. A lock-file/named-mutex alternative would put two mechanisms in play that must agree about who is the instance.

**Implications**: `PANEL_BANNER` is part of the module's contract. Bind failure is **fail-open** (a panel with no single-instance protection beats no panel), and `release()` deletes `panel.port` only when it still holds this instance's port — the same self-owned-resource rule the daemon applies to `.linkwatcher.lock`.

### Decision 11: Phase G hardening — three changes

**Date**: 2026-08-10 (Integration & Testing, Phase G)
**Context**: The hardening pass had to make two surfaces testable that the "no widget instantiation" rule had left unreachable, and had to close the accelerator gap carried over from Phase E.

**(a) The dispatcher drain moved to a module-level `drain_queue(queue, log)`.** INT-8 covers "any worker"; the UI-thread half of the isolation rule lived inside `ControlPanelApp._pump_dispatch_queue`, and the class builds a `Tk()` in `__init__`, so the test spec's "Tk display: avoided" rule made it unreachable. The drain is now a pure function the pump calls — the same display-free seam the rendering rules use (Decision 6). Behavior is unchanged; the extraction is what makes the rule assertable.

**(b) A pending discovery is no longer rendered as an empty registry.** Found by the Phase G live smoke run, not by any test: the first `psutil` pass on this machine took **7.5 s** for 409 processes (subsequent passes 0.11 s), and for that whole window the list pane asserted *"No registered projects found."* while the status bar said *"0 daemons running"*. Both are claims about reality the panel had not yet observed — the same falsehood the truthful-state rule forbids for row status (TDD §4.3), in the pessimistic direction. `list_pane_display` gained a fourth mode (`loading` → "Discovering daemons…"), selected while `model.last_refresh is None`; a real error still outranks it, so an unresolved registry never sits on a spinner forever. The cold-start cost itself is not a defect (steady-state polls are well inside the §7.1 budget) and is handed to Performance & E2E Test Scoping, which owns per-feature performance tests.

**(c) Toolbar accelerators route through button enablement.** `Alt+S`/`Alt+T`/`F5` fire their handler only when that button is enabled, so the COMP-1 matrix governs the keyboard exactly as it governs the mouse — a keystroke can never trigger an action the UI shows as unavailable.

---

### Implementation Patterns Used

**State Management Pattern**:

- Pattern: Single observable model (`AppModel`) with one-way data flow; worker threads → thread-safe queue → UI-thread dispatcher (`root.after`)
- Why: keeps Tk single-threaded and the rendered state truthful (poll-observed or deadline-bounded pin)
- Where: `model.py`, `app.py`, `discovery.py`, `views/`

**Error Handling Pattern**:

- Pattern: per-surface pane-local error states; worker exceptions caught, logged to the panel log, rendered — never propagated to the mainloop
- Why: one project's/one surface's failure never takes down the panel or other rows
- Where: every external-surface module (`discovery`, `lifecycle`, `validation`, `config_edit`, `log_tail`)

**Data Flow Pattern**:

- Pattern: external reality (registry + locks + psutil + logs) → poller → queue → model → views; user actions dispatch operations whose observed results update the model
- Why: views render from the model only; actions never mutate widget state directly
- Where: the full poll cycle and every user action (TDD §4.3)

---

## 10. Issues & Resolutions Log

> Populate when issues arise during work on this feature (bug fixes, blockers, investigations). For formal bug reports, use [bug-tracking.md](../../../doc/state-tracking/permanent/bug-tracking.md); this section provides feature-local context.

**2026-08-10 (Phase F) — three deployment defects, all found by the Step 16 "verify non-test-suite artifacts" pass and all fixed in-session.** None was visible to the 1180-test suite; each would have shipped a broken install. Fixed here rather than filed as separate bugs (human partner's call at the Phase F checkpoint).

| # | Defect | How it surfaced | Fix |
| - | ------ | --------------- | --- |
| 1 | `psutil` declared only under `[project.optional-dependencies] test`, so the install venv — built from runtime deps — lacked it and `discovery.py` raised `ModuleNotFoundError` on every poll. The shipped panel would have shown a permanently empty daemon list ("Discovery failed: No module named"). Invisible to tests, which inject a fake process table by design; invisible in the dev repo, where the test extra is installed. | Live hook-wrapper run against a scratch install | Moved to runtime `dependencies` in `pyproject.toml`; verified a venv built from that list enumerates the real daemons. |
| 2 | `GitPython` was **never** declared, though `main.py` imports it (`from git import ...`, inside a `try`, which is why a line-anchored grep missed it). A venv built from the declared list failed `main.py --help` outright. It only ever worked because deployed venvs carried gitpython from an earlier dependency set. | Installer smoke test failing after defect 1's fix | Added `GitPython>=3.1.0` to runtime `dependencies`; venv verification now asserts every declared runtime dep instead of an ad-hoc trio. |
| 3 | **Pre-existing** (`where = ["src"]` is byte-identical at HEAD): `create_linkwatcher_venv` installs deps via `pip install <install_dir>`, but the install tree is flat while the copied `pyproject.toml` declares a src layout — `error in 'egg_base' option: 'src' does not exist`. With no `requirements.txt` anywhere in the repo, the working branch never ran, so **any fresh install aborted at the venv step**. Existing installs predate the breakage, which is why it had not surfaced. | Isolated installer driver against a clean scratch dir | `install_linkwatcher` now generates `requirements.txt` from the pyproject runtime deps (tomllib, with a line-scan fallback for < 3.11), so the requirements branch is taken and no build of the flat tree is attempted. |

Verified after the fixes: a clean scratch install runs copy → generated `requirements.txt` → venv created and verified → four wrapper scripts → both `--help` smoke tests, and discovery from that installed tree enumerates the live daemons.

**2026-08-10 (Phase G) — one display defect, found by the live smoke run and fixed in-session.** Folded in here rather than filed as a product bug (human partner's call at the Phase G checkpoint), on the Phase F precedent: it never shipped, and the fix landed in the same session that found it.

| # | Defect | How it surfaced | Fix |
| - | ------ | --------------- | --- |
| 4 | The list pane rendered the EC-8 empty state — *"No registered projects found."*, with *"0 daemons running"* in the status bar — during the window between launch and the first poll landing. On this machine that window measured **7.5 s** (409 processes; steady-state polls 0.11 s), so the panel spent seven seconds asserting something it had not observed. Root cause: the view had no way to distinguish *"discovery has not reported yet"* from *"discovery reported nothing"*, so "no data" fell through to the empty-registry branch. Invisible to the whole suite — every test applies a poll before asserting, so the pre-first-poll state was never rendered. The same falsehood the truthful-state rule (TDD §4.3) forbids for row status, in the pessimistic direction. | Scripted live run of the composed app against a synthetic registry (the run that also confirmed the extracted dispatcher) | `list_pane_display` gained a fourth mode, `loading` → "Discovering daemons…", selected while `model.last_refresh is None`; the status bar shows the same text instead of a daemon count. A real error still outranks it, so an unresolved registry never sits on a wait state forever. Covered by three COMP-8 cases plus the live smoke's pre-first-poll assertions. |

**2026-08-17 (Code Review, PF-TSK-005) — quality-validation record; verdict 🔄 Needs Enhancement.** Method: eight dimension reviewers over the full change set (19 panel modules, `control_panel.py`, `deployment/install_global.py`, `pyproject.toml`), each finding then adversarially verified against source — **28 confirmed, 25 refuted**. `process-framework/tools/linkWatcher/start_linkwatcher_hook_wrapper.ps1` was **excluded from review scope** (framework path, PF-TSK-005 scope guard) and routes to Process Improvement. The feature is unreleased (`~/bin` carries no `control_panel.py`), so findings below are **implementation gaps**, not bugs — the one exception, a pre-existing installer defect, was filed as [PD-BUG-121](../permanent/bug-tracking.md).

#### Quality Validation Results (decomposed-mode record)

| Area | Result |
| ---- | ------ |
| Acceptance criteria | **10 of 12 MET.** AC-11 PARTIAL — single-instance surfacing MET, hook auto-open UNMET (CR-1). AC-12 PARTIAL — persist + restart notice MET, invalid-value explanation broken for type-invalid values (CR-3). |
| Business rules | 7 MET; **BR-8 UNMET** (CR-1); BR-9 PARTIAL (CR-3). |
| TDD key requirements | KR-01 / KR-02 / KR-03 MET; KR-04 PARTIAL (CR-1); KR-05 PARTIAL (CR-3). |
| §9 Decisions 2–11 | All implemented as described except D9b (save-pipeline exception coverage — CR-3). |
| Performance vs TDD §7.1 targets | **Met.** Measured live 2026-08-17: steady-state process enumeration **~100 ms / 364 processes** (budget <500 ms); convergence ≈ 2.6 s (target ≤ 5 s); cold first pass 8.7 s, consistent with the accepted 7.5 s limitation. The identity test run against this machine matched **exactly** the live daemon pair — three-way identity and pair resolution verified against reality, not only fixtures. Gap: the §7.1 DEBUG instrumentation is absent (CR-13). |
| Test suite | 1239 passed, 3 skipped, 4 xfailed (exit 0). flake8 clean at the enforced commit gate (`--max-line-length=120 --extend-ignore=E203`), isort clean. Panel coverage: `model.py` and `views/rendering.py` 100%; discovery 94%, validation 95%, config_edit 93%, log_tail 91%, single_instance 90%, settings 86%, lifecycle 85%; `app.py` 36% and widget modules 17–20% by the test spec's no-widget-instantiation rule. |
| Dependencies | `psutil>=5.9.0` (moved to runtime) and `GitPython>=3.1.0` (newly declared) both genuinely imported, both BSD-3 / MIT-compatible. Python 3.9 floor re-verified by AST parse at `feature_version=(3,9)` — clean. GitPython floor admits CVE-bearing versions (CR-18). |
| Accepted Known Limitations | All five re-verified as genuinely implemented as claimed. |
| Positives | 68 recorded. Notably: kill-time re-verification closes the poll→kill PID-recycling window; path comparison is normalized *equality*, never substring; `cleanup_lock` deletes only a self-owned lock after confirmed exit; **no `shell=True` anywhere**; the single-instance protocol is bounded (64-byte cap, one verb, loopback, banner handshake); the threading contract holds at all five worker-creation sites; double-exit is structurally impossible rather than timing-dependent; `save_config` validates the exact persisted bytes and leaves the file byte-identical on all four refusal paths; the log-tail incremental UTF-8 decode is correct at every constructible boundary. |

#### Open Implementation Gaps (status OPEN — for Feature Enhancement, PF-TSK-069)

| ID | Sev | Finding | Location |
| -- | --- | ------- | -------- |
| CR-1 | 🟠 Major | **Hook auto-open (FR-8 / UI-7 / BR-8 / AC-11 first clause) is not implemented.** `start_linkwatcher_hook_wrapper.ps1` carries no `pythonw` / `control_panel` / `Open-ControlPanel` reference in the working tree, at HEAD, or anywhere in history. Almost certainly implemented and live-verified on 2026-08-10 and then **silently reverted by the 2026-08-14 framework rollout** (`.framework-version` 2026-07-02-001 → 2026-08-14-001), invisible because the feature was untracked in git until 2026-08-17. §5 inventory, §10, §2 and TDD §4.5/§9 all claim it delivered — **those records are corrected by this entry**. Re-implementation routes to Process Improvement (framework path); a local edit will be reverted by the next rollout. | `process-framework/tools/linkWatcher/start_linkwatcher_hook_wrapper.ps1` |
| CR-2 | 🔴 Critical | **Cross-project config destruction.** `read_config` catches only `OSError`, so a non-UTF-8 byte raises `UnicodeDecodeError` out of its documented "never raises (EC-10)" contract; `_load()` commits `self._project_id` and the header *before* the read and leaves `_mode`/`_baseline`/editor content stale when it raises. Header names project B, editor holds project A's YAML, `_mode` is still `"ok"`, Save is armed, and `save()` resolves the path from the switched id — **A's config is written over B's** and the pane reports "Saved." Exposure at review: **0 corruptible today, 3 mutation-exposed** (3 registered configs valid UTF-8, 1 absent, panel not deployed). Every other panel read uses `errors="replace"`. | `views/config_pane.py:124` + `config_edit.py:92` |
| CR-3 | 🟠 Major | **EC-11 / KR-05 breach**: `save_config` catches only `OSError`, so type-invalid YAML (e.g. `log_level: 5`) makes the real validator raise `AttributeError`/`TypeError` out of the save pipeline — no blocked-save notice, no log entry. | `config_edit.py:132` |
| CR-4 | 🟠 Major | `is_daemon_for` claims a **`main.py --validate` scan** as the project's daemon (no executable-name or run-mode discrimination). A stopped project renders RUNNING, Start is disabled, and Stop or window-close terminates the in-flight scan, truncating the report. Breaches the module's own D-T5 invariant; trigger is a workflow CLAUDE.md tells agents to run routinely. | `discovery.py:150` |
| CR-5 | 🟠 Major | `extract_project_root`'s raw-string regex fallback claims a **wrapper shell** whose single quoted argument merely mentions `main.py … --project-root` — the shape of `bash.exe -c '…'` and the documented `pwsh -Command '& …'` form. The fallback is dead weight for real daemons (psutil always returns split argv). | `discovery.py:136` |
| CR-6 | 🟠 Major | **Arrow-key list navigation is inert** — `selection_set()` without `focus()`, so Tk's `Keynav` no-ops while item focus is `""`. No row is selectable without a mouse; breaches the Critical UX dimension's keyboard-operability requirement. | `views/main_window.py:273` |
| CR-7 | 🟡 Minor | Refresh / F5 has no in-flight guard; poll results are applied in completion order with no sequence number, so a stale snapshot can overwrite a newer one. | `app.py:204`, `model.py:181` |
| CR-8 | 🟡 Minor | DRAINING pin TTL (grace + 5 s = 25 s) is shorter than its own worker's designed worst case (~30 s), so a row can revert to Running mid-stop and re-enable a Stop whose click the busy guard then swallows. | `lifecycle.py:578` |
| CR-9 | 🟡 Minor | Launcher spawned as PATH-relative `pwsh.exe`; Windows `CreateProcess` searches the panel's current directory first. | `lifecycle.py:184` |
| CR-10 | 🟡 Minor | Non-finite panel settings (`.inf` / `.nan`) pass `_coerce_positive_number` and kill the discovery thread via `OverflowError` raised outside the try — the daemon list freezes on its first snapshot with no warning. | `settings.py:122` |
| CR-11 | 🟡 Minor | A non-UTF-8 `panel-config.yaml` raises `UnicodeDecodeError` out of `load_panel_settings` — the panel never opens and logs nothing, despite a "Never raises" contract. Same class as CR-2. | `settings.py:196` |
| CR-12 | 🟡 Minor | Every successful Start leaks two undeletable `%TEMP%` files (CRT opens without `FILE_SHARE_DELETE`, so the `finally` unlink fails and is swallowed); five pairs from 2026-08-10 were still present on 2026-08-17, contradicting the inline "cleaned up by the OS" justification. | `lifecycle.py:130` |
| CR-13 | 🟡 Minor | TDD §7.1 DEBUG instrumentation (poll-cycle duration, dispatcher queue depth) is not implemented — exactly one `.debug()` call exists in the whole subpackage, while `--debug` advertises verbose logging. | subpackage-wide |
| CR-14 | 🟡 Minor | A transient registry read failure clears the selection, which reloads the config pane and **discards unsaved edits**. | `model.py:188` |
| CR-15 | 🟡 Minor | Save advertises an `Alt+S` mnemonic (`underline=0`) that ttk never activates, while `<Alt-s>` on the toplevel **starts a daemon** — pressing it from the editor launches instead of saving. | `views/config_pane.py:88` |
| CR-16 | 🟡 Minor | Status bar reports "Project registry not found" for registry-parse and discovery failures too, contradicting the on-screen banner; those errors also never reach the panel log the status bar tells the user to read. | `views/main_window.py:288` |
| CR-17 | 🟡 Minor | Installer panel smoke test renders its failure reason from `stderr` while `control_panel.py` prints the dependency diagnostic to `stdout`, so a real failure prints an empty reason; same one-line issue at the pre-existing daemon smoke test. | `deployment/install_global.py:608` |
| CR-18 | 🔵 Suggestion | `GitPython>=3.1.0` floor admits versions carrying CVE-2023-40590 (Windows untrusted search path for `git.exe`) and CVE-2023-41040 — reachable in shape, since `Repo(project_root)` runs on Windows against user-supplied roots. Installed 3.1.44 is unaffected; raise the floor to `>=3.1.41`. | `pyproject.toml` |
| CR-19 | 🔵 Suggestion | No Tk `report_callback_exception` override — a UI-thread callback exception bypasses the panel log and vanishes entirely under the `pythonw` launch. | `app.py:97` |
| CR-20 | 🔵 Suggestion | Installer interpolates the install path into a single-quoted PowerShell literal; an apostrophe in the home directory silently breaks the generated wrapper. | `deployment/install_global.py:114` |

#### Gap Closure Outcomes (Feature Enhancement, PF-STA-111)

> Per-finding disposition for the CR table above. Recorded as a companion table
> rather than an extra column on the 20-row table, so each outcome carries its
> verification evidence; the gaps table stays as Code Review wrote it. Empty rows
> mean not yet reached — sessions 2 and 3 are outstanding.

| ID | Session | Outcome | What changed | Verification |
| -- | ------- | ------- | ------------ | ------------ |
| CR-2 | 1 (2026-08-17) | ✅ Fixed | **Both halves.** `config_edit.read_config` now reports a non-UTF-8 file as `error` (`read_text` raises `UnicodeDecodeError`, a `ValueError`, which escaped the `OSError`-only handler) and deliberately does *not* decode leniently, since lossy text would be written back on the next save. `views/config_pane.render` additionally fails closed if `_load()` raises for any reason: state fields land before widget calls, so `_mode` can never stay `"ok"` with the previous project's baseline while Save resolves the switched project's path | UNIT-C7 (2 cases) + a scripted real-Tk run, 15/15 checks — dirty editor on project A, switch to a raising project B, asserted `_mode != "ok"`, baseline cleared, Save disabled, `save()` refuses, no exception escaping `render()`, then recovery. Mutation-verified |
| CR-3 | 1 (2026-08-17) | ✅ Fixed | `save_config` now converts a raise from `LinkWatcherConfig.from_file()`/`validate()` into a blocked save with an operator-facing explanation and a `panel.log` entry, instead of letting it escape and take the unsaved edits with it. `OSError` is re-raised so the outer handler still owns genuine I/O failure | UNIT-C8 (4 type cases + 1 discriminating case proving ordinary `validate()` issues still take the issue path). Byte-identical-on-refusal and no-temp-residue asserted per case. Mutation-verified |
| CR-4 | 2 (2026-08-18) | ✅ Fixed | `is_daemon_for` gained a run-mode clause: any flag that makes `main.py` exit rather than run the watcher (`--validate`, `--version`, `--help`) disqualifies the process, making identity four-way. D-T5 and TDD §4.4 step 2 amended to match | UNIT-D11 (4 mode cases + 5 guard cases proving ordinary daemon flags still qualify). **Verified against the live process table**: with the pre-fix logic a real `main.py --validate` scan of this project was claimed alongside the genuine daemon pair (3 PIDs for one project); with the fix, only the pair. Mutation-verified |
| CR-5 | 2 (2026-08-18) | ✅ Fixed | The raw-argv regex fallback in `extract_project_root` now applies only to a genuine one-token command line. psutil always returns a split argv, so re-joining it could never help a real daemon — it could only invent targets | UNIT-D12 (3 wrapper shapes: `bash -c`, `pwsh -Command`, `cmd /c` + a guard keeping the documented one-token raw form parseable). Honest scope note: on the live table this was **latent** — the wrapper shells present mention `main.py` but not `--project-root`, so no false positive was firing at that moment. Mutation-verified |
| CR-6 | 2 (2026-08-18) | ✅ Fixed | Two halves: `selection_set()` is now paired with `focus()` (Tk key navigation follows the *focus item*, which was `""`), and a `<FocusIn>` handler adopts the first row when the list is focused with nothing focused — without it a keyboard-only operator had no entry point at all | Scripted real-Tk run, 17/17 checks: Down/Down/Up traverse rows, focus adoption selects and propagates to the model, and model-driven selection sets item focus |
| CR-7 | 2 (2026-08-18) | ✅ Fixed | Poll results now carry a sequence stamped at poll **start** (so ordering reflects when reality was observed, not when the poll finished) and the model drops any result older than the newest applied, error included. Manual Refresh also gained an in-flight guard — every F5 press used to spawn another unbounded thread | UNIT-M9 (stale result, stale error, newer-still-applies, unsequenced-callers-unaffected) + UNIT-D11-adjacent sequence-monotonicity test. Mutation-verified |
| CR-8 | 2 (2026-08-18) | ✅ Fixed | `DRAIN_PIN_MARGIN_SECONDS` is now derived as `EXIT_WAIT_TIMEOUT_SECONDS + 5`, so the DRAINING pin (grace + 15 s) outlives the stop worker's worst case (grace + 10 s). It previously expired at grace + 5 s — 5 s *before* the worker could finish | UNIT-L12, asserted across three grace periods rather than the one in use. Mutation-verified |
| CR-9 | 2 (2026-08-18) | ✅ Fixed | New `resolve_shell()` walks PATH directly and skips every relative entry, so Windows' current-directory-first `CreateProcess` search cannot supply the launcher shell; falls back to the bare name (logged) rather than losing Start on an odd machine | UNIT-L13 + guard cases. **Measured**: `shutil.which` is *not* a usable fix — it returned the cwd decoy even when given an explicit `path=`, because it unconditionally prepends `os.curdir` on Windows. Mutation-verified |
| CR-10 | 1 (2026-08-17) | ✅ Fixed | `_coerce_positive_number` is now total: the `float()` conversion is guarded (an over-range integer literal raised `OverflowError` from the conversion itself) and non-finite values are rejected. `.inf`/`.nan` previously passed every gate — both are `float` instances and neither satisfies `value <= 0` | UNIT-S7 (4 non-finite cases + over-range integer + a discriminating case proving legitimate values, including ints, still apply with no warnings). Confirmed the downstream mechanism directly: `Event.wait(inf)` raises `OverflowError`. Mutation-verified (2 separate mutations) |
| CR-11 | 1 (2026-08-17) | ✅ Fixed | `load_panel_settings` catches `UnicodeDecodeError` and returns defaults plus a warning, honouring its documented "never raises" contract; previously the panel never opened and logged nothing | UNIT-S7 (non-UTF-8 case). Mutation-verified |
| CR-12 | 2 (2026-08-18) | ✅ Fixed | Each run now sweeps launcher capture files left by earlier runs (aged past an hour), bounding an accumulation the panel cannot prevent at source — the spawned daemon inherits the handle and the CRT opens without `FILE_SHARE_DELETE`. The false "cleaned up by the OS" justification was removed from the docstring | UNIT-L14 + a guard that an in-flight capture survives. **Measured on this machine**: 8 leaked files up to 7.9 days old were present and were swept. Mutation-verified |
| CR-13 | 3 (2026-08-18) | ✅ Fixed | The TDD §7.1 instrumentation now exists: dispatcher queue depth logged at DEBUG when non-empty (silent when idle, or an idle panel writes ten lines a second), and poll-cycle duration logged per pass — raised to WARNING past the §7.1 500 ms budget so a responsiveness problem is visible without re-running under `--debug`. Both live in module-level Tk-free functions so they are assertable without a display | INT-10 (4 cases incl. the idle-silence guard and the WARNING escalation). Mutation-verified (2 separate mutations) |
| CR-14 | 1 (2026-08-17) | ✅ Fixed | `AppModel.apply_poll` no longer treats a no-rows-plus-error result as "zero projects": rows, selection, pins and `last_refresh` are preserved, since a poll that raised observed nothing. Agrees with `list_pane_display`'s existing "rows win" precedence and with the already-established loading-vs-empty distinction | UNIT-M8 (3 preservation cases + 2 discriminating cases: a *successful* empty poll still clears, and rows-with-an-error still apply). Mutation-verified — the refresh case was found **vacuous** on first attempt (two real `datetime.now()` calls landed in one Windows clock tick and compared equal) and was made deterministic with an injected `refreshed_at` |
| CR-15 | 2 (2026-08-18) | ✅ Fixed | Removed the `underline=0` from the config-pane Save button. It advertised an `Alt+S` mnemonic ttk never activates, while `<Alt-s>` on the toplevel starts a daemon — so the advertised key launched instead of saving. PD-UIX-003 assigns `Alt+S` to Start and `Ctrl+S` to Save, so this is code-into-conformance; no spec change | Scripted real-Tk run: `underline == -1` and `Ctrl+S` still bound on the editor |
| CR-16 | 2 (2026-08-18) | ✅ Fixed | Two halves: the status bar reports the *actual* error instead of a fixed "Project registry not found" that contradicted the banner beside it; and registry-load failures now reach `panel.log` — they previously reached no log at all, because `load_projects` returns its three errors rather than raising and only the catch-all logged. Logged on *change* so a persistent problem is one line, not one per poll, and recovery is recorded too | Real-Tk run across all three registry error shapes plus discovery failure; UNIT-D-level tests for log-once-per-change and recovery logging. Mutation-verified |
| CR-17 | 3 (2026-08-18) | ✅ Fixed | New `smoke_failure_reason()` renders from stderr, falls back to stdout, and finally names the exit code — so a reason is never empty. `control_panel.py` reports dependency failures with a bare `print()`, i.e. on stdout, which is why the stderr-only rendering printed nothing. Applied to both smoke tests | UNIT-P1 (5 cases). **Verified against a real install**: the panel smoke test was forced to fail in the installed tree and printed the actual diagnostic. Mutation-verified |
| CR-18 | 3 (2026-08-18) | ✅ Fixed | `GitPython>=3.1.41`, above CVE-2023-40590 (Windows untrusted search path for `git.exe`) and CVE-2023-41040 | UNIT-P1. **Verified end to end**: the generated `requirements.txt` carries the raised floor and a fresh scratch venv resolved 3.1.59. Mutation-verified |
| CR-19 | 3 (2026-08-18) | ✅ Fixed | `root.report_callback_exception` now routes to the panel log with the traceback. Tk's default hook prints to stderr, which is attached to nothing under the `pythonw.exe` launch, so these vanished entirely. Complements `drain_queue`: that isolates callbacks the panel *queues*, this covers handlers Tk invokes directly (button commands, key bindings, `<<TreeviewSelect>>`), which never pass through the dispatcher | INT-10, incl. a case proving the last-resort handler cannot itself raise. Mutation-verified |
| CR-20 | 3 (2026-08-18) | ✅ Fixed | The install path is escaped (apostrophe doubled) before interpolation into the single-quoted PowerShell literal in `find_venv_daemon_processes` | UNIT-P1 (3 cases, one asserting the escape lives in the installer rather than only in the test). **Measured**: the unescaped form is a genuine `pwsh` parse error, so the enumeration silently matched nothing for any user whose home directory contains an apostrophe. Mutation-verified |
| CR-1 | — | ⛔ Out of scope | Framework path — a local edit is reverted by the next rollout | Filed as **PF-IMP-2032** (Process Improvement). Gates the feature reaching 🟢 Completed; AC-11/FR-8/UI-7/BR-8 stay UNMET until it lands |

**Root cause routed outward (session 1).** CR-3's underlying defect is not in the panel:
`LinkWatcherConfig.validate()` and `_from_dict` neither coerce nor type-check, so a wrongly
typed value raises instead of being reported in the issue list `validate()` exists to return.
Measured 2026-08-17 across four value types. The daemon is exposed too — `main.py:453` calls the
same `validate()` during startup, so instead of the precise `config_issue` lines written
immediately below that call, an operator gets a generic `fatal_error` with a raw Python message
and exit 1. Filed against feature 0.1.3 as **[PD-BUG-123](../permanent/bug-tracking.md)**. The
panel fix above is still required independently: a pane must handle anything an external module
throws, which is its own CQ-Critical per-surface error-handling obligation.

**2026-08-18 (Code Review, PF-TSK-005) — repeat review of the gap-closure delta; verdict 🔎 PASSED.**
Scope: the fix delta only (16 files, +1311/-89 vs HEAD `006f5a7`, which is the pre-review state),
per PF-TSK-005's repeat-review scoping rule — the previously passed scope was seam-checked, not
re-reviewed. Unrelated in-flight work in the same dirty tree (`path_resolver.py`,
`reference_lookup.py`, `utils.py`, PD-BUG-120/121) was excluded; `install_global.py` carries both
CR-17/CR-20 and the PD-BUG-121 removal, which were separated.

**All 19 product-path findings (CR-2…CR-20) verified fixed against source**, not accepted on the
recorded evidence. Checks that held: CR-4's exit-flag list is genuinely complete against `main.py`'s
argparse surface; `DRAIN_PIN_MARGIN_SECONDS` (15 s) does equal `WATCHDOG_MARGIN_SECONDS`; CR-9's
sibling spawn site (`validation.py`) was already safe via `sys.executable`; CR-12's sweep verified
live — 0 leftover captures against 8 before. Suite 1338 passed / 0 failed (was 1239), flake8 clean
across the delta.

#### Quality Validation Results (decomposed-mode record, post-closure)

| Area | Result |
| ---- | ------ |
| Acceptance criteria | **11 of 12 MET** (was 10). AC-12 now MET — CR-3 closed. AC-11 still PARTIAL: single-instance surfacing MET, hook auto-open UNMET (CR-1). |
| Business rules | 8 MET; BR-9 now MET (CR-3); **BR-8 still UNMET** (CR-1). |
| TDD key requirements | KR-01/02/03/05 MET — KR-05 closed by CR-3; **KR-04 still PARTIAL** (CR-1). |
| Performance vs TDD §7.1 | **Met.** Steady-state ~100 ms against the 500 ms budget; CR-13 now instruments it. |
| Test suite | 1338 passed, 3 skipped, 4 xfailed (exit 0); framework runner 1332 + 6 deselected, green. flake8 clean for the delta. Coverage 82% overall — panel logic modules 86–100% (`model.py` 100%), view modules 16–20% by the test spec's no-widget-instantiation rule. |
| Verdict | **PASS → 🔎 Needs Test Scoping.** Every residual gap traces to CR-1 alone, which is framework-path, cannot be closed in product code, and is tracked as PF-IMP-2032 — it gates 🟢 Completed, not this gate. Returning the feature to 🔄 Needs Enhancement would leave no product-path work to do. |

#### Open Implementation Gaps from this review (status OPEN — feature is unreleased, so these are gaps, not bugs)

| ID | Sev | Finding | Location |
| -- | --- | ------- | -------- |
| CR-21 | 🟡 Minor | **CR-4 and CR-5 disagree on the token model, leaving CR-4's hole open in the path CR-5 kept alive.** The run-mode guard matches whole tokens (`token.casefold() in _NON_DAEMON_MODE_FLAGS`); the retained one-token regex fallback never presents whole tokens. Probed: a split-argv `--validate` scan is correctly refused, but the same scan as a single unsplit command-line string is still claimed as the daemon — and so is eligible for termination, CR-4's original consequence. Either that path is reachable (CR-4 incompletely fixed) or it is not (CR-5's retained branch is dead code); it cannot be both. Latent today because psutil returns a split argv. | `discovery.py:162-180` |
| CR-22 | 🟡 Minor | **CR-13 logs an expected condition at WARNING, deviating from the TDD.** §7.1 specifies poll-duration/queue-depth at DEBUG; §7.3 scopes the WARN+ panel-log record to forced terminations, start failures, validation failures and worker exceptions. Cold start is an *accepted* Known Limitation measured at 7.5–8.7 s, so every panel launch now writes a WARNING into the "why was my daemon force-stopped" record for documented-normal behavior. The TDD was amended for CR-4/CR-5 and CR-1 in this delta but not for this. Resolve in either direction: lower to DEBUG, or amend §7.1/§7.3. | `discovery.py:428-437`, TDD §7.1/§7.3 |
| CR-24 | 🔵 Suggestion | `PollResult.sequence` defaults to `0` rather than `None`, so `getattr(result, "sequence", None)` in `app._on_poll_result` can never yield `None`. Any future result built off the module-level `poll_once()` would be stamped 0 and then silently, permanently dropped by the CR-7 staleness check once sequencing has started. Unreachable today — every app path goes through `DiscoveryPoller.poll_once()`. `Optional[int] = None` fails safe. | `discovery.py:361-366`, `app.py:232` |

**Routed outward.** *(a)* The order-dependence of the delta's panel-log assertions was already filed
as **[PD-BUG-125](../permanent/bug-tracking.md)** with the mechanism unpinned; this review pinned it
— `caplog` captures through the root logger, `setup_panel_log()` sets `propagate = False`, and
`caplog.at_level(..., logger=...)` sets only the level, never propagation. One fixture in
`test_panel_integration.py` restores `propagate = True` on teardown; nothing else does, and 7
`caplog.at_level` sites depend on that. Reproduced deterministically in a two-test probe; it did
*not* reproduce in three full-suite runs on 2026-08-18, so it is order-sensitive, not resolved.
*(b)* The absence of automated regression protection for the view-layer fixes — CR-2's `_fail_closed`
(the last defense against the Critical cross-project overwrite), CR-6, CR-15, CR-16's status bar, all
in modules at 16–17% coverage, evidenced only by one-off scripted real-Tk runs that were not retained
as artifacts — is **pre-existing architecture, not introduced by this delta**, and is filed as
technical debt rather than held against it.

**Positives.** Root-cause discipline held — CR-3's underlying loader defect was filed as PD-BUG-123
rather than absorbed. The PD-BUG-121 removal in the same file is residue-free and pinned by AST
assertions rather than source-text matching, exactly as the project's Script Development Quick
Reference prescribes. Audit statuses were correctly returned to `🔄 Needs Audit` on content change
instead of carrying forward a verdict granted against different content. The gap-closure record's
self-reported evidence was accurate at every point it was independently checkable.

### Known Limitations & Tech Debt

| Item | Type | Priority | Mitigation | Tracked In |
| ---- | ---- | -------- | ---------- | ---------- |
| Log-idle drain is a heuristic — a move-correlation buffered inside `move_detect_delay` may be silent and still dropped at force-terminate | Known Limitation | Low | Atomic writes bound damage to *dropped* (never corrupted) updates; 20 s grace covers one full 10 s correlation window (accepted by FDD BR-7) | TDD PD-TDD-033 §9 Known Issues |
| A STARTING row at window close is not drained — a daemon whose launcher completes after the panel exits keeps running unmanaged | Known Limitation | Low | **Revisited in Phase G (2026-08-10): keeping it, deliberately.** Adding STARTING to the shutdown targets makes things worse, not better — the controller's busy guard rejects the stop dispatch for a project whose start is in flight, so the row never leaves `STILL_DRAINING_STATUSES`, close waits for the watchdog (grace + 15 s ≈ 35 s) *and the daemon survives anyway*. A correct fix needs a re-dispatch loop in the one path that must never hang, which is disproportionate for a case that requires closing inside the few-second start window and whose harm equals ordinary hook-started-daemon behavior (the daemon is fully independent; reopening the panel lists and manages it) | This file (§9 Decision 8) |
| Two launches within the same few milliseconds can both acquire the single-instance slot, yielding two windows (never more — the later writer owns `panel.port`, so the other becomes unreachable to future launches) | Known Limitation | Low | Sub-millisecond race window; both windows are visible to the operator and either can be closed. EC-9's real harm (conflicting start/stop) requires the operator to drive both. Closing it would need a second exclusion primitive alongside the socket | This file (§9 Decision 10, checkpoint-approved 2026-08-10) |
| The hook wrapper's panel auto-open is absent entirely (**not** merely missing on the early-exit paths) | 🔴 Open gap — see §10 CR-1 | — | **The "RESOLVED" claim below was invalidated by the 2026-08-14 framework rollout, which reverted the uncommitted 2026-08-10 change; Code Review confirmed on 2026-08-17 that no panel reference exists in the file at HEAD or in history.** *Original entry, retained for the record:* **RESOLVED (Phase G, 2026-08-10)**: the auto-open is now an `Open-ControlPanel` function invoked on all three exit paths. The original rationale ("LinkWatcher failed to start, so the panel has nothing to supervise") does not hold for the timeout path — an 8 s timeout means the wrapper stopped waiting, not that the daemon failed, and the panel is exactly how the operator finds out. Verified end-to-end against a scratch install with a real venv: all three paths launch the panel with the derived `--project-root`, and the timeout path still returns at 8.7 s with stdout captured through a pipe | This file (§9 Decision 10) |
| The panel's first `psutil` pass is slow on a busy machine — measured at 7.5 s for 409 processes (steady-state polls: 0.11 s), so the list is empty for several seconds after launch | Known Limitation | Low | The window now shows an explicit "Discovering daemons…" wait state instead of falsely claiming an empty registry (§9 Decision 11b), so the cost is visible rather than misleading. Steady-state polling is well inside the TDD §7.1 budget. Per-feature performance tests are owned by Performance & E2E Test Scoping (PF-TSK-086) — hand this measurement to it as the cold-start data point | This file (§9 Decision 11b) |

---

## 11. Next Steps

> Populate during active work on this feature. Update at the end of every work session.

**Last Updated**: 2026-08-17

1. ~~**Phase G — hardening**~~ — **DONE (2026-08-10)**: EC-1…EC-11 swept (traceability table in TE-TSP-045 v1.3; EC-7 pipeline half and COMP-8 were the two gaps), INT-8 across four worker surfaces plus the dispatcher, COMP-2/COMP-8, the Alt+S/Alt+T/F5 accelerators, the hook wrapper's all-exit-paths auto-open,. One defect found and fixed in-session (the pending-discovery false empty state).
2. ~~**Implementation Finalization (PF-TSK-055)**~~ — **DONE (2026-08-13)**: see §3.
3. 🔴 **Commit the feature — [Git Commit and Push (PF-TSK-082)](../../../process-framework/tasks/07-deployment/git-commit-and-push.md). Do this before Code Review.** Discovered at Implementation Finalization: **the entire feature is untracked in git** — all 19 panel source modules, all 10 test files, `control_panel.py`, all five design docs (FDD / UI Design / TDD / Test Spec / Implementation Plan) and *this state file* have never been `git add`ed. They are **not** gitignored (`git check-ignore` reports no rule); they were simply never added. 7.1.1 is the only one of nine features whose state file is untracked and the only untracked `src/linkwatcher/` subpackage — every sibling is tracked, so this is an anomaly, not the project's normal commit lag. Consequences until fixed: (a) roughly six weeks of work exists only in the working tree, with no recovery point — and this project has a recorded incident (2026-03-17) where a `git stash` destroyed hundreds of uncommitted changes; (b) Code Review has no diff to review; (c) the feature cannot ship — a release builds from committed state.
4. ~~**Code Review (PF-TSK-005)**~~ — **DONE (2026-08-17): verdict 🔄 Needs Enhancement.** 28 findings confirmed / 25 refuted; full quality-validation record and the CR-1…CR-20 gap table in §10. One pre-existing installer defect filed separately as [PD-BUG-121](../permanent/bug-tracking.md).
5. ~~**Feature Enhancement (PF-TSK-068)**~~ — **DONE (2026-08-17 → 2026-08-18)**: all 19 product-path findings closed across three sessions under [PF-STA-111](../temporary/old/enhancement-code-review-gap-closure.md). Session 1 the contract-escape class (CR-2, 3, 10, 11, 14), session 2 identity/lifecycle/keyboard (CR-4, 5, 6, 7, 8, 9, 12, 15, 16 + TD263), session 3 observability and packaging (CR-13, 17, 18, 19, 20). Design docs moved with the code: TDD → v1.7, Test Spec → v1.7 (83 scenarios). Every added assertion mutation-verified.
6. 🔴 **Process Improvement (PF-TSK-009) for [PF-IMP-2032](../../../process-framework-central/state-tracking/permanent/process-improvement-tracking.md)** — re-implement the hook auto-open in `start_linkwatcher_hook_wrapper.ps1` (framework path). **This gates the feature**: FR-8 / UI-7 / BR-8 / AC-11-first-clause stay UNMET until it lands, so 7.1.1 cannot legitimately reach `🟢 Completed` and a re-review will bounce on the same criterion.
7. 👀 **Code Review (PF-TSK-005) — re-review of record.** Focus areas handed over: CR-2's fix at both halves (reader contract *and* pane-state ordering); the `except`-clause sweep verified as a class rather than four instances; the CR-4/CR-5 identity change against the D-T5 invariant (including that unattributable processes are still never claimed); that the 68 recorded positives on these modules did not regress; and that AC-11 is recorded as UNMET rather than raised as a new finding.
8. **Then** — Performance & E2E Test Scoping (PF-TSK-086 — hand it the cold-start measurement: 7.5 s at Phase G, re-measured 8.7 s / 364 processes on 2026-08-17), then User Documentation Creation (PF-TSK-081) for the three `❌ Needed` rows in §4.

> **Release note for whoever runs the next release**: the Phase F dependency fixes (`psutil`, `GitPython`) and the generated `requirements.txt` change what a fresh install produces. Existing installs will not pick the panel up until reinstalled — `~/bin` currently has no `control_panel.py`, so the hook's auto-open call is silently skipped there by design.

### Open Questions

- ~~**Frozen-project filtering**~~ — **RESOLVED (Phase B, 2026-07-31): no filtering; the panel lists every registry entry.** `version_freeze` governs framework rollouts, not daemon supervision — appdev is frozen and runs a daemon. Filtering would hide a live daemon from its supervisor. TDD §8 Q2 updated. (See also Decision 5 for the missing-project-root case.)
- ~~**Broken-link count extraction**~~ — **RESOLVED (Phase E, 2026-08-10): report path from stdout's locator line, count from the report file's `Broken links  : N` line, stdout fallback.** TDD §8 Q3 updated. (See §9 Decision 9.)

### Follow-ups Raised During Phase B (not this feature's scope)

- **Launcher singleton guard shares the case-sensitivity defect** — `Test-LinkWatcherAlreadyRunning` in `start_linkwatcher_background.ps1` matches with `.Contains($ProjectRoot)`, which is case-sensitive. If the launcher is invoked with a differently-cased project root than the running daemon's command line carries (exactly the `C:` vs `c:` divergence observed here), the guard misses and a duplicate daemon can be spawned — the multi-instance condition PF-IMP-967 exists to prevent. Not fixed here: the panel never spawns daemons directly (D-T4), and the file sits on the framework path, so it needs routing via the Issue Classification and Routing Guide rather than a product bug report. Raised with the human partner 2026-07-31. **Re-verified at Implementation Finalization (2026-08-13)**: the defect is still present (`.Contains($ProjectRoot)`, `start_linkwatcher_background.ps1` line 102) and is filed **nowhere** — absent from both the open central improvement tracker and its archive (searched for `Test-LinkWatcherAlreadyRunning` / `case-sensitiv` / `start_linkwatcher_background`; the nearby rows PF-IMP-967, PF-IMP-1612 and PF-IMP-1682 all address different failure modes). Human partner's decision at the finalization checkpoint: **do not file** — retained here as the record.
- **`doc/technical/architecture/design-docs/tdd/` is untracked in git** — the whole directory, so no TDD in this project is under version control. Noticed while amending PD-TDD-033; outside this task's scope. **Superseded at Implementation Finalization (2026-08-13)**: the problem is broader than the TDD directory — the *entire* 7.1.1 feature is untracked. See §11 item 3, which is the actionable form of this note.

- **`New-SourceStructure.ps1 -Update` cannot regenerate this project's Directory Tree (framework-path script defect)** — found at Implementation Finalization while updating PD-ARC-001. Its `$existingTreePattern` requires the auto-generated marker block to be followed by a bare-LF blank line (`...\n\n` + fence), but `source-code-layout.md` is CRLF throughout (187 CRLF, 0 bare LF), so `[^\n]*` consumes the `\r`, the second `\n` never matches, and the script exits with *"Warning: Could not find Directory Tree section pattern"* and `Changes: 0` — a silent no-op that reports success. Any CRLF checkout of this doc is affected, so the tree has been un-regenerable for some time and now omits `linkwatcher_control_panel/` entirely. Not fixed here: the script sits on the framework path, so it routes through Support Tasks (Process Improvement), not this product task. The **manual** sections of PD-ARC-001 — which the script explicitly preserves and never touches — were updated by hand at this task instead (Fixed-Rules deviation note, Dependency Flow + import-rules row, the new Panel Subsystem subsection, and the File Placement decision tree).

- **`Update-FeatureImplementationState.ps1` cannot set `👀 Needs Review` without faulting (framework-path script defect)** — found at Implementation Finalization while performing this task's own prescribed state transition. Its `switch ($Status)` at line 201 maps a feature status to a test status but has branches for only **three of the four** values its own `ValidateSet` (line 63) accepts; `👀 Needs Review` has none, so `$testStatus` is empty and line 207 throws *"Cannot bind argument to parameter 'Status' because it is an empty string."* The line 197 comment ("Update 2: Test Tracking (**if test status needs updating**)") shows the intent was to *skip* when there is nothing to map — the branch and the null-guard are both simply missing. Notable because `👀 Needs Review` is the status **Implementation Finalization (PF-TSK-055) Step 10 exists to set**, so this task has evidently never been able to complete its own state update through the canonical script. Order of operations makes it survivable: `feature-tracking.md` is written at line 188, *before* the fault, so the transition lands; what the fault skips is the trailing `Update-WorkflowTracking.ps1` call (run separately here — no-op) and the summary. Exit code is 1. Not fixed here (framework path → Support Tasks). A secondary, harmless warning from the same run: `AdditionalUpdates column 'Implementation Completion' not found` — this project's feature table has no such column, so `-CompletionDate` is silently dropped.
