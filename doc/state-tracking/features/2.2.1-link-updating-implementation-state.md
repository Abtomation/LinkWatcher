---
id: PD-FIS-051
description: "Feature implementation state tracking for feature 2.2.1 Link Updating."
type: Process Framework
category: Feature Implementation State
version: 2.1
created: 2026-02-21
updated: 2026-07-12
feature_name: Link Updating
implementation_mode: Retrospective Analysis
feature_id: 2.2.1
status: Retrospective Analysis
consolidates:
  - PF-FEA-021 (2.2.1 Link Updater)
  - PF-FEA-022 (2.2.2 Relative Path Calculation)
  - PF-FEA-023 (2.2.3 Anchor Preservation)
  - PF-FEA-024 (2.2.4 Dry-Run Mode)
  - PF-FEA-025 (2.2.5 Backup Creation)
workflows:
  - WF-001
  - WF-002
  - WF-004
  - WF-005
  - WF-007
  - WF-008
---

# Link Updating - Implementation State

> **📖 Craft home**: the [`feature-implementation-planning` skill](../../../.claude/skills/feature-implementation-planning/SKILL.md) — initialization and living-document maintenance craft, activated from Feature Implementation Planning's Check Recommended Skills step.
>
> **🚨 NEVER archived**: instances of this template are permanent feature documentation for the feature's entire lifecycle — finalization sets status `COMPLETE` in place (policy consumed by Code Review, Implementation Finalization, and Release & Deployment).
>
> **Retrospective Analysis mode** (onboarding tasks [PF-TSK-064](../../../process-framework/tasks/00-setup/codebase-feature-discovery.md), [PF-TSK-065](../../../process-framework/tasks/00-setup/codebase-feature-analysis.md), [PF-TSK-066](../../../process-framework/tasks/00-setup/retrospective-documentation-creation.md)):
> - Section 3 tracks analysis progress rather than planned tasks
> - Section 5 (Code Inventory) is the primary deliverable — every file must be assigned
> - Section 7 documents decisions discovered in code, not planned decisions
> - All content is descriptive ("what is") rather than prescriptive ("what should be")

> **v2.0 Consolidation**: This file replaces 5 former feature states (PF-FEA-021 through PF-FEA-025, old 2.2.1–2.2.5) as part of the 42→9 feature consolidation.
---

## 1. Feature Overview

### Feature Description

The Link Updating feature handles the actual modification of files to update references when a target file has moved. The core class `LinkUpdater` (`updater.py`) receives a list of affected `LinkReference` objects along with separate `old_path` and `new_path` strings via `update_references()`, then updates each source file to reflect the new target location. Path resolution logic is delegated to `PathResolver` (`path_resolver.py`).

Key capabilities include:
- **Relative path calculation**: Computes the correct new relative path from each source file to the moved target's new location
- **Bottom-to-top line sorting**: Processes link replacements from the bottom of a file upward, ensuring line number offsets remain valid even when replacements change line lengths
- **Atomic writes**: Writes changes to a temporary file first, then renames it over the original — preventing partial writes if the process is interrupted
- **Backup creation**: Creates `.bak` copies of files before modification
- **Dry-run mode**: Logs what would change without modifying any files
- **Anchor preservation**: Preserves `#fragment` anchors when updating link paths
- **Link-type dispatch**: Different update strategies for markdown links, YAML values, JSON values, Python imports, etc.

### Business Value

- **User Need**: When a file moves, all references must be updated accurately and safely — no broken links, no data loss.
- **Business Goal**: Zero-risk automated link updates with rollback capability.
- **Success Metrics**: All affected references updated correctly; no file corruption; backups available for rollback; dry-run shows accurate preview.

### Scope

**In Scope**:

- `LinkUpdater` class with file modification logic
- Relative path calculation from source to moved target
- Bottom-to-top line processing for stable offsets
- Atomic write-then-rename file modification
- Backup file creation (`.bak`)
- Dry-run mode (preview without modification)
- Anchor/fragment preservation
- Link-type-aware replacement strategies

**Out of Scope**:

- Move detection (→ 1.1.1 File System Monitoring)
- Link discovery (→ 2.1.1 Link Parsing System)
- Link storage (→ 0.1.2 In-Memory Link Database)

---

## 2. Current State Summary

**Last Updated**: 2026-08-17
**Current Status**: MAINTAINED — blueprint-aware reference updating enhancement implemented (PF-STA-110); Performance & E2E Test Scoping complete 2026-07-15 → 🟢 Completed (feature-tracking); PD-BUG-114 simultaneous-move link repair fixed 2026-08-10 → 🔒 Closed 2026-08-18 (Code Review approved, PF-TSK-005) - review filed PD-BUG-124 (Medium: the restored separator is a bare backslash for backslash-authored paths, which inside a .py or .json string literal escapes the closing quote and writes an unterminated string literal - the class was introduced by PD-BUG-118 and its reach extended here) and TD270 (Low: the utils.py AI Context inventory omits the newly shared function and its two callers); PD-BUG-116 dry-run within-moved-file rewrite gap fixed and 🔒 Closed 2026-08-10 (S-scope quick path); PD-BUG-118 path-span corruption (docstring columns, trailing separator, swallowed punctuation) fixed 2026-08-17, Code Review approved → 🔒 Closed 2026-08-17 — review filed PD-BUG-120 (High: same trailing-separator loss survives in the within-moved-file recalculation path) and TD268 (no column-span verification at the position-replacement choke point); PD-BUG-119 move-memory thread safety (unguarded `_recent_moves` / `_pending_recalcs` across the observer and DirectoryMoveDetector threads) fixed 2026-08-17, Code Review approved → 🔒 Closed 2026-08-17 — review filed PD-BUG-122 (Medium: the check-then-register pair still spans two lock acquisitions, so a registration can be orphaned under an already-applied key) and TD269 (the failure-mode-2 regression pin no longer discriminates after the fix hoisted its `time.monotonic()` hook above the lock); PD-BUG-120 authored trailing separator stripped from links inside a moved file fixed 2026-08-17 → 🔒 Closed 2026-08-18 (Code Review approved, PF-TSK-005) - review filed PD-BUG-124 (Medium: the restored separator is a bare backslash for backslash-authored paths, which inside a .py or .json string literal escapes the closing quote and writes an unterminated string literal - the class was introduced by PD-BUG-118 and its reach extended here) and TD270 (Low: the utils.py AI Context inventory omits the newly shared function and its two callers) — `_apply_trailing_separator_style` promoted from a `PathResolver` static method to shared `utils.apply_trailing_separator_style` and applied at both return points of `reference_lookup._calculate_updated_relative_path`, closing the half PD-BUG-118's choke-point fix could not reach
**Current Task**: Test Audit of the 14 enhancement tests (PF-TSK-030) — the remaining release gate
**Completion**: 100% for v1.0 scope (fully deployed, in production use); enhancement pending test audit + release

### What's Working

- [✓] Relative path calculation for all supported formats
- [✓] Bottom-to-top line sorting for safe in-place editing
- [✓] Atomic writes via temp-file-then-rename
- [✓] Backup creation before modification
- [✓] Dry-run mode with accurate preview
- [✓] Anchor preservation in link updates
- [✓] Link-type dispatch for format-specific replacement
- [✓] Override-aware virtual-root resolution (v1.1, 2026-07-06): `path_resolution_overrides` applies to the live move/update path — host-absolute `/…` references inside override folders (blueprint) are matched via the folder's base and rewritten preserving virtual-root style; verified by real-blueprint dry-run (417 files, 0 errors)

### What's In Progress

- Test Audit (PF-TSK-030) of the 14 enhancement tests (13 + suffix-hijack regression) — the remaining release gate (audit gate noted 2026-07-06)

### What's Blocked

_Nothing blocked._

---

## 3. Implementation Progress

### Retrospective Analysis Progress

> **Note**: This feature was implemented before the process framework was established.

- [✅] **PF-TSK-064**: Codebase Feature Discovery — Code Inventory populated (2026-02-17)
- [✅] **PF-TSK-065**: Codebase Feature Analysis — Dependencies and Design Decisions documented (2026-02-18)
- [✅] **PF-TSK-066**: Retrospective Documentation Creation — Tier Assessment (ART-ASS-196), FDD (PD-FDD-027), TDD (PD-TDD-026) created (2026-02-19/20)
- [✅] **Feature Consolidation**: Merged 2.2.1–2.2.5 into unified feature, new state file created (2026-02-21)
- [✅] **PF-TSK-022**: Code Refactoring — TD129: Added `update_references_batch()` and `_update_file_references_multi()` to LinkUpdater for batched directory-move file writes. Each referring file is opened/written at most once per directory move. (PD-REF-126) (2026-03-30)
- [✅] **PF-TSK-022**: Code Refactoring — TD242: PD-BUG-095 disk-existence guard in `path_resolver._calculate_new_target_relative` prefix branch now delegates to shared helper `path_exists_under_root()` (utils.py), consolidating with the mirror PD-BUG-033 guard in `reference_lookup`. Pure consolidation, no behavior change. (PD-REF-207) (2026-04-29)
- [✅] **PF-TSK-022**: Code Refactoring — TD251: Phase-1 idempotency guard for PYTHON_IMPORT in `updater._replace_at_position`. Replaced unbounded `line.replace(ref.link_text, new_import_text)` with bounded regex `(?<![.\w]) + re.escape(ref.link_text) + (?!\w)`, mirroring Phase 2's PD-BUG-094 pattern. Re-applying the same replacement is now a safe no-op — defense-in-depth against PD-BUG-096-class duplicate-ref hazards. Multi-format-file-move integration narrative updated to reflect new pattern. 3 new unit tests in `TestPythonImportIdempotency`. (PD-REF-215) (2026-04-29)
- [✅] **PF-TSK-022**: Code Refactoring — TD252: PD-BUG-098 follow-ups in `updater.py` — (1) clarified `_filter_contained_overlaps` docstring that only strictly-contained overlap is handled (partial overlap is not addressed); (2) Option C: ambiguous-fallback skip in `_replace_at_position` (invalid columns + multiple occurrences) now increments per-file `_ambiguous_skip_count` aggregated into `UpdateStats["errors"]` by `update_references` / `update_references_batch`, surfacing previously-silent skips without the whole-file blast of escalating to STALE; (3) shortened two long warning-log `reason=` lines via implicit string concatenation; (4) replaced bare `set` annotation with `typing.Set[int]` for consistency with surrounding `Dict`/`List`/`Tuple` annotations. 1 new test `test_ambiguous_fallback_increments_errors_count`; 0 regressions. (PD-REF-219) (2026-04-29)
- [✅] **PF-TSK-068**: Feature Enhancement — Blueprint-aware reference updating (PF-STA-110): extended `path_resolution_overrides` virtual-root resolution from validate-only into the live move/update path. New shared module `resolution_overrides.py` (normalize + longest-prefix folder match, consumed by both `Validator` and `PathResolver` — behavior-preserving validator refactor); `PathResolver` gained `path_resolution_overrides` constructor param (wired from config via `service.py` → `LinkUpdater`), per-source-file resolution-base cache, base-aware input resolution in `_resolve_to_absolute_path`, base-stripping output reconstruction in `_convert_to_original_link_type` with `path_exists_under_root` containment guard and `update_resolution_override_applied` OB event. Quality-audit hardening: early-exit equality/prefix branches are skipped for virtual-root links so a real root-level move coinciding with a blueprint virtual path cannot hijack the link. +13 tests (11 unit incl. byte-for-byte v1.0 regression anchors + 2 anti-hijack; 2 integration end-to-end). Real-blueprint dry-run: file rename 162 refs / dir move 1648 refs proposed, 0 errors, tree byte-identical. Full suite 962 passed, 0 regressions. Design: PD-TDD-026 v1.1 / PD-FDD-027 v1.1 / TE-TSP-040 v1.1. (2026-06-29 design, 2026-07-06 implementation)
- [🔄] **PF-TSK-005**: Code Review — Blueprint-aware reference updating change set (2026-07-06). **Quality-validation results**: automated checks — black clean, full suite 968 passed / 0 failed, coverage of changed modules resolution_overrides 100% / validator 100% / updater 98% / path_resolver 85%. Acceptance criteria (PD-FDD-027 v1.1): FR-8 ✅, FR-9 ✅, BR-9 ✅, AC-7 ✅ (all with test evidence); **BR-8 ⚠️ unmet at one edge** — the Major finding below. Performance vs TDD targets: no explicit targets defined for the enhancement; PE consideration (per-source-file base cache, no per-reference recomputation) verified in code. SE path-traversal containment, DI guard retention (atomic write/backup/PD-BUG-095/112), validator refactor behavior-preservation, and non-override byte-for-byte invariance all re-verified independently — PASS. Findings: 🟠 1 Major (residual suffix hijack of virtual-root links, §9 Open Implementation Gaps — reproduced empirically); 🔵 4 Suggestions (recorded in §9 gap entry); 🟢 Positives: shared-helper extraction (EM), deliberate lossy-v1.0 regression anchor, real-blueprint dry-run diligence. **Verdict: Needs Enhancement** — fix the gap, then re-review. *Test-tracking note*: the 2.2.1 test-file rows carry Status "🔄 Needs Update" — set incidentally by `Update-FeatureImplementationState.ps1`'s status mapping during this review's tracking updates, and kept deliberately (human partner decision 2026-07-06): the 13 new enhancement tests have not passed Test Audit, and the audit gate requires that before release. Test Audit (PF-TSK-030) owns the re-audit and the final status.
- [✅] **PF-TSK-007**: Bug Fixing (bug-fix-style follow-up, 2026-07-12) — closed the code-review Major finding (§9): gated the PD-BUG-045 suffix block in `path_resolver._calculate_new_target_relative` on `not is_virtual_root_link` (one-condition fix, matching the approved fix shape). Regression test `test_override_link_not_hijacked_by_suffix_coinciding_move` added to `TestOverrideAwareResolution` — failed pre-fix with the exact hijack signature (`tasks/foo-task.md`, style lost), passes post-fix; v1.0 no-config anchor unaffected (gate is override-scoped). PD-TDD-026 corrected ("only match path" sentence + "no double-fire" bullet rewritten to describe the gate); TE-TSP-040 scenario (f) added. Similar-issue check: Python-import suffix block not exposed (import targets never host-absolute); database.py Phase-2 suffix match is candidate-collection only. Full suite 963 passed / 0 failed; black clean. No PD-BUG filed (finding tracked here per human partner routing 2026-07-06). Manual validation test skipped — automated regression reproduces the exact scenario. → 👀 Needs Review for Code Review repeat cycle.
- [✅] **PF-TSK-005**: Code Review repeat cycle (2026-07-12) — gap-fix delta PASSED. **Quality-validation results**: gate at path_resolver.py:214 verified correct, minimal, and matching the approved fix shape; **class closure verified independently** — all three unresolved-form comparison sites (equality, directory-prefix, suffix) share one gating condition, Step-2 resolution is base-aware and all Step-3 matchers (`_match_direct`/`_match_stripped`/`_match_resolved`) compare only the resolved form, filename-only fallback unreachable for `/…` links, so PD-TDD-026's corrected "Step-3 is the only match path for virtual-root links" claim is now factually true. Genuine PD-BUG-045 nested-project case preserved (relative suffix links never start with `/`; non-override sources get base `""` → byte-for-byte v1.0, anchor test green). Python-import sibling suffix block independently confirmed not exposed (PYTHON_IMPORT targets are dotted-module slash forms, never host-absolute). Regression test traced statically to the pre-fix hijack signature (`tasks/foo-task.md`) — faithful repro; documented fail-then-pass consistent. Tests: scoped files 60/60; full suite 955 passed / 0 failed (3 skipped, 4 xfailed pre-existing; count-composition difference vs the fix session's 963, no failure discrepancy); black clean. TDD + test-spec scenario (f) wording verified accurate against shipped code. **Findings**: 0 Critical / 0 Major / 0 Minor; 🔵 1 Suggestion — path_resolver module "AI Context" docstring (lines 16-32) still describes the pre-v1.1 flow (no override-aware base resolution, no virtual-root gating of early exits + suffix match); routed to Code Refactoring lightweight path per human partner 2026-07-12. **Process note**: `Update-CodeReviewState.ps1 -ReviewStatus "Completed"` maps to non-legend "✅ Review Completed" — status transition performed via the mutation-guide-blessed `Update-FeatureTrackingStatus` helper instead; script drift filed centrally as PF-IMP-1470. **Verdict: Approved** → 🔎 Needs Test Scoping. Release still gated by Test Audit of the 14 enhancement tests (PF-TSK-030).
- [✅] **PF-TSK-086**: Performance & E2E Test Scoping (2026-07-15) — scoped the PF-STA-110 enhancement delta. **Performance**: added one Level 2 operation benchmark **BM-009** (⬜ Needs Creation) — override-aware move handling has zero benchmark coverage (all existing updater benchmarks BM-004/PH-001/004/005/006 run override-free); matrix Q1/Q3/Q4 not triggered (no parser/db module touched; per-source-file base cache bounds the added work; non-override path is byte-for-byte v1.0, regression-anchored, covered by existing baselines). **E2E**: discovered and added untracked workflow **WF-011** "Override-folder move → virtual-root links updated" (P2; features 0.1.3/1.1.1/0.1.2/2.1.1/2.2.1, all ready 5/5) — the config→live-update-path interaction is new (previously validate-only, WF-009). Milestone added (⬜ Not Created; test-case creation downstream via PF-TSK-069). Existing WF-001/002/004/005/007/008 cases left as-is — they exercise the non-override path (regression-anchored byte-for-byte), so results remain valid. Feature → 🟢 Completed (user docs already updated for v1.1). **Script defect found & filed**: `New-PerformanceTestEntry.ps1` emits pre-Metric 12-column rows against the now-13-column table (Metric added v1.1 2026-04-28); BM-009 row hand-realigned to restore alignment; filed as PF-IMP-1534 for Process Improvement.

---

## 4. Documentation Inventory

### Design Documentation

| Document | Type | Status | Location | Last Updated |
| -------- | ---- | ------ | -------- | ------------ |
| Tier Assessment (ART-ASS-196) | Tier Assessment | Complete | [ART-ASS-196](../../documentation-tiers/assessments/PD-ASS-196-2-2-1-link-updating.md) | 2026-02-20 |
| FDD (PD-FDD-027) | Functional Design | Complete (v1.1 — override-folder reference maintenance) | [fdd-2-2-1-link-updater.md](../../functional-design/fdds/fdd-2-2-1-link-updater.md) | 2026-06-29 |
| TDD (PD-TDD-026) | Technical Design | Complete (v1.1 — override-aware path resolution) | [tdd-2-2-1-link-updater-t2.md](../../technical/tdd/tdd-2-2-1-link-updater-t2.md) | 2026-07-06 |
| Test Spec (TE-TSP-040) | Test Specification | Complete (v1.1 — override-aware resolution scenarios) | [test-spec-2-2-1-link-updating.md](../../../test/specifications/feature-specs/test-spec-2-2-1-link-updating.md) | 2026-06-29 |
| [TE-TSP-046](/test/specifications/cross-cutting-specs/cross-cutting-spec-e2e-override-folder-move-scenarios.md) | Test Specification | ✅ Created | test/specifications/cross-cutting-specs/cross-cutting-spec-e2e-override-folder-move-scenarios.md | 2026-08-10 |

### User Documentation

| Document | Type | Status | Location | Last Updated |
| -------- | ---- | ------ | -------- | ------------ |
| Capabilities Reference | Handbook (shared) | Updated for v1.1 — override applies to live updates, not just `--validate` | [linkwatcher-capabilities-reference.md](../../user/handbooks/linkwatcher-capabilities-reference.md) | 2026-07-06 |
| Configuration Guide | Handbook (shared) | Updated for v1.1 — § Per-Folder Path Resolution Override covers live updating + safety guards | [configuration-guide.md](../../user/handbooks/configuration-guide.md) | 2026-07-06 |
| Link Validation / Quick Reference | Handbooks (shared) | Validate-only wording corrected | [link-validation.md](../../user/handbooks/link-validation.md), [quick-reference.md](../../user/handbooks/quick-reference.md) | 2026-07-06 |

### Quick Links

- **Main Design**: [TDD (PD-TDD-026)](../../technical/tdd/tdd-2-2-1-link-updater-t2.md)
- **Related Features**: [1.1.1 File System Monitoring](1.1.1-file-system-monitoring-implementation-state.md), [2.1.1 Link Parsing System](./2.1.1-link-parsing-system-implementation-state.md)

---

## 5. Code Inventory

### Files Created by This Feature

| File Path | Purpose | Key Components | Status | Created |
| --------- | ------- | -------------- | ------ | ------- |
| [linkwatcher/updater.py](../../../src/linkwatcher/updater.py) | Link update engine | `LinkUpdater` class — text replacement, atomic write, backup, dry-run; delegates path resolution to PathResolver; passes `path_resolution_overrides` through (v1.1) | Existing | Pre-framework |
| [linkwatcher/path_resolver.py](../../../src/linkwatcher/path_resolver.py) | Path resolution | `PathResolver` class — target path calculation, link type analysis, match strategies, relative path conversion; override-aware virtual-root resolution (`path_resolution_overrides` param, `_resolution_base_for` + cache, base-aware input resolution, base-stripping output reconstruction, `update_resolution_override_applied` event) (v1.1) | Existing | TD033 refactoring |
| [linkwatcher/resolution_overrides.py](../../../src/linkwatcher/resolution_overrides.py) | Shared virtual-root override helpers | `build_resolution_overrides()`, `resolution_base_for_rel()` — single normalize + longest-prefix-match algorithm consumed by both `Validator` (6.1.1) and `PathResolver` (2.2.1) | New | PF-STA-110 enhancement (2026-07-06) |

### Test Files

| Test File | Type | Coverage Areas | Status | Created |
| --------- | ---- | -------------- | ------ | ------- |
| [test/automated/unit/test_updater.py](../../../test/automated/unit/2-link-parsing-update/2-0-link-parsing-update/test_updater.py) | Unit | Path calculation, replacement, backup, dry-run | Existing | Pre-framework |
| [test/automated/integration/test_link_updates.py](../../../test/automated/unit/2-link-parsing-update/2-0-link-parsing-update/test_link_updates.py) | Integration | End-to-end update scenarios | Existing | Pre-framework |

---

## 6. Dependencies

### Feature Dependencies

**This Feature Depends On**:

- **[PF-FEA-046: Core Architecture](0.1.1-core-architecture-implementation-state.md)** (MAINTAINED)
  - Why: Uses `LinkReference`, `FileOperation` data models and `PathResolver` for path resolution
  - Impact if unavailable: Cannot represent links or compute new paths

- **[PF-FEA-047: In-Memory Link Database](0.1.2-in-memory-link-database-implementation-state.md)** (MAINTAINED)
  - Why: Receives affected link references queried from database
  - Impact if unavailable: No links to update

- **[PF-FEA-052: Logging System](./3.1.1-logging-system-implementation-state.md)** (MAINTAINED)
  - Why: Logs all update operations for auditability
  - Impact if unavailable: No update audit trail

**Other Features Depend On This**:

- **[PF-FEA-049: File System Monitoring](1.1.1-file-system-monitoring-implementation-state.md)** (MAINTAINED)
  - Why: Handler triggers updater when a move is detected
- **[PF-FEA-046: Core Architecture](0.1.1-core-architecture-implementation-state.md)** (MAINTAINED)
  - Why: Service holds the updater instance

### System Dependencies

**Required Packages**:

| Package | Version | Purpose | Added |
| ------- | ------- | ------- | ----- |
| colorama | ≥0.4 | Colored dry-run output | Pre-framework |
| (stdlib) | — | `os`, `shutil`, `tempfile`, `pathlib` | Pre-framework |

---

## 7. Dimension Profile

> **Reference**: [Development Dimensions Guide](/process-framework/guides/framework/development-dimensions-guide.md)
>
> **Source**: Backfill (dimension-aware development integration) | **Last reviewed**: 2026-07-06 (reaffirmed during PF-STA-110 enhancement)

### Applicable Dimensions

| Dimension | Importance | Key Considerations |
|-----------|-----------|-------------------|
| Security & Data Protection (SE) | Critical | Modifies files — must prevent path traversal, validate paths. v1.1: config-supplied override base is normalized and containment-guarded (`path_exists_under_root`); a `..`-bearing base cannot produce an out-of-root rewrite (audited 2026-07-06) |
| Data Integrity (DI) | Critical | Atomic writes (temp+rename), backup creation, no partial writes on failure. v1.1: override rewrites anchored on equality with the actually-moved path + disk-existence guard; early exits skipped for virtual-root links (anti-hijack); non-override resolution byte-for-byte invariant (regression-anchored) |
| Extensibility & Maintainability (EM) | Relevant | Link-type dispatch for different update strategies |
| Performance & Scalability (PE) | Relevant | Batch updates should minimize I/O; bottom-to-top processing |
| Observability (OB) | Relevant | File modifications need audit logging |

### Not Applicable

| Dimension | Rationale |
|-----------|-----------|
| Accessibility / UX Compliance (UX) | Internal component — no UI |

---

## 8. Design Decisions

### Decision 1: Bottom-to-Top Line Processing

**Date**: Pre-framework (discovered in code)
**Context**: When replacing link text in a file, if a replacement changes the line count or character offsets, subsequent replacements on earlier lines would use stale line numbers.

**Decision Made**: Sort replacements by line number descending and process from bottom to top

**Rationale**: Changes at the bottom of a file don't affect line numbers above. Processing bottom-to-top ensures all line numbers remain valid throughout the replacement process.

**Implications**:

- Reliable even when replacements change line lengths
- Slightly more complex sorting step, but guarantees correctness

---

### Decision 2: Atomic Write via Temp-File-Then-Rename

**Date**: Pre-framework (discovered in code)
**Context**: If the process crashes during a file write, the original file could be left in a corrupted state.

**Decision Made**: Write all changes to a temporary file, then rename it over the original (atomic on most file systems)

**Rationale**: On NTFS (Windows), file rename is atomic. If the process crashes during write, the temp file is discarded and the original remains intact.

**Implications**:

- Requires write permission for temp file in the same directory
- Slight overhead from double-write, but ensures data safety

---

### Decision 3: Backup Before Modification

**Date**: Pre-framework (discovered in code)
**Context**: Even with atomic writes, users may want to revert changes.

**Decision Made**: Create `.bak` copy of each file before modification

**Rationale**: Provides an additional safety net. Users can manually restore from `.bak` files if needed.

---

### Decision 4: Dry-Run Mode

**Date**: Pre-framework (discovered in code)
**Context**: Users want to preview what would change before committing to modifications.

**Decision Made**: Configurable dry-run flag that logs all intended changes without modifying files

**Rationale**: Zero-risk preview. Essential for first-time setup and configuration validation.

---

### Implementation Patterns Used

**Safe File Modification Pattern**:

- Pattern: Temp-file write → rename (atomic swap)
- Why: Crash-safe file modification
- Where: `LinkUpdater._write_file_safely()`

**Processing Order Pattern**:

- Pattern: Bottom-to-top line sorting
- Why: Stable line offsets during multi-replacement
- Where: `LinkUpdater._update_file_references()`

---

## 9. Issues & Resolutions Log

### Open Implementation Gaps

_None open. (Resolved gaps below.)_

| Status | Found | Summary | Fix Shape |
| ------ | ----- | ------- | --------- |
| **RESOLVED 2026-07-12** (PF-TSK-007 follow-up; fix as prescribed — suffix block gated on `not is_virtual_root_link`, regression test added and verified fail-then-pass, TDD sentence corrected; full suite 963/0) | 2026-07-06 (Code Review PF-TSK-005) | **Residual virtual-root hijack via the PD-BUG-045 suffix block.** The quality-audit anti-hijack fix skips the two early-exit branches in `path_resolver._calculate_new_target_relative` for virtual-root links (`is_virtual_root_link`), but the PD-BUG-045 suffix-match block further down still compares the *unresolved* link form against `old_path` and remains reachable when base-aware Step-3 resolution doesn't match. Reproduced with the canonical config shape: a blueprint link `/tasks/foo.md` (true virtual target did not move) is rewritten to `tasks/foo-task.md` — wrong target and virtual-root style lost — when an unrelated suffix-coinciding file (`<override>/…/tasks/foo.md`) is renamed. Violates PD-FDD-027 BR-8 at this edge; PD-TDD-026 v1.1's "Step-3 base-aware resolution is the only match path for virtual-root links" overclaims. **Empirical exposure scan (2026-07-06, FrameworkBuilder appdev — the only deployment with overrides configured)**: 0 currently-valid virtual-root links corruptible; 7 already-dangling links in 4 blueprint files would receive lossy style-breaking rewrites; exposure set regenerates with every restructure (the feature's core use case). | Gate the suffix block on the existing flag: `if not is_virtual_root_link and old_norm.endswith(suffix_tag):` — plus a regression test mirroring the repro (source in override folder, link whose true virtual target doesn't move, suffix-coinciding file moved) and correction of the TDD sentence. The existing `test_no_override_config_keeps_v1_0_behavior` anchor is unaffected (gate is override-scoped). |

**Code-review suggestions recorded with the gap (🔵, no separate tracker entries per human partner routing 2026-07-06):** (1) moved-outside-virtual-root fallback writes `/on-disk-path`, which override machinery reinterprets as `<base>`-relative — deliberate least-bad fallback per TDD, observation only; (2) `stripped == base` edge in `_convert_to_original_link_type` would yield link `/` — near-impossible trigger; (3) `LinkUpdater.__init__` `path_resolution_overrides` passthrough lacks `Optional[Dict[str, str]]` annotation; (4) pre-existing: `utils.path_exists_under_root` doesn't reject `..` in candidates (safe with current handler-derived callers). **From the 2026-07-12 repeat review:** (5) path_resolver module "AI Context" docstring lags the v1.1 flow (override-aware base resolution and virtual-root gating undocumented) — resolved same-day via Code Refactoring lightweight path per human partner routing.

### Bug Fix History

| Bug ID | Summary | Fix Date | Fix Description |
| ------ | ------- | -------- | --------------- |
| PD-BUG-012 | Link text not updated when it matches old target path | 2026-03-03 | Modified `_replace_markdown_target` regex to capture link text as separate group; when text exactly matches `ref.link_target`, it is updated to new target alongside the URL |
| PD-BUG-095 | `path_resolver._calculate_new_target_relative` directory-prefix early-exit had no existence guard, so regex strings whose backslashes were normalized to forward slashes (e.g. `'doc/foo/bar-\d+'` → `'doc/foo/bar-/d+'`) incidentally matched the moved-directory prefix and got rewritten on directory moves | 2026-04-28 | Added existence guard mirroring PD-BUG-033's check in `reference_lookup._calculate_updated_relative_path`: if the proposed new target doesn't exist on disk, return original target unchanged. Pairs with parser-level rejection in `utils.looks_like_regex_or_glob()` (see 2.1.1 bug history) for defense in depth |
| PD-BUG-096 | Directory move with multiple Python imports double-prefixed import statements: when `utils/` moved to `src/utils/` and a single source file imported both `utils.a` and `utils.b`, the import lines became `import src.src.utils.a` while usage sites correctly became `src.utils.a` — internally inconsistent. PYTHON_IMPORT refs were returned by both `find_references(file_path)` (via the resolved-target index) and `get_references_to_file(module_path)`, so each import ref reached `_replace_in_line` twice and `_replace_at_position`'s unbounded `str.replace` substring-matched inside the already-prefixed result | 2026-04-29 | `reference_lookup.collect_directory_file_refs` now filters PYTHON_IMPORT refs out of `file_references` for `.py` files, keeping them only in `module_references` with the correct module-path target. Each ref reaches Phase 1 exactly once. Regression test `test_bug094_dir_move_multi_import_no_double_prefix` (previously xfail) flipped to passing |
| PD-BUG-098 | Reference-update sweep produced overlapping-substring corruption when two refs on the same line had nested column ranges (inner contained in outer): descending-column processing replaced the inner ref first and extended the line, then the outer ref's stale column positions sliced into the freshly-inserted text — producing `<new_target> + chopped_tail` like `LinkWatcherBrokenLinks.txtols/linkWatcher/LinkWatcherBrokenLinks.txt`. Whole-line staleness check at updater.py:324 missed this because `link_target` was still in the line, just not at the original column. Affected 17 files during one move including production source. Secondary risk: invalid-position fallback in `_replace_at_position` used unbounded `line.replace(old, new)` that would multi-replace if `link_target` appeared twice on the line | 2026-04-29 | Added `_filter_contained_overlaps` pre-pass to `_apply_replacements` that drops refs whose column range is strictly contained in another ref on the same line (with `overlapping_references_resolved` warning log). Replaced unbounded `line.replace` in invalid-position fallback with occurrence-counting bounded form (0=no-op, 1=replace once, >1=skip with `ambiguous_fallback_replacement_skipped` warning). 4 regression tests in `test_updater.py::TestOverlappingReferenceCorruption` (inner-contained-in-outer, triple-nested overlap, non-overlapping sanity, invalid-columns bounded fallback) — all FAILED before the fix with exact bug signatures, all PASS after |
| PD-BUG-112 | On a directory move, `path_resolver._calculate_new_target_relative`'s early-exit and directory-prefix branches reconstructed the rewritten target from a forward-slash-normalized form, flipping **every** backslash in the original string to `/` — including string-literal escape sequences. A Python literal `"...\n...\t..."` referenced from a moved-prefix line became `"...​/n...​/t..."`, breaking source (SyntaxError at feedback_db.py); `"blueprint\core"` became `"blueprint2/core"`. The existing PD-BUG-095 existence guard did not catch it because the relocated target genuinely existed on disk; the damage was the separator normalization itself, not a bad match | 2026-06-22 | Added `PathResolver._apply_separator_style(normalized_path, original_target)` (mirrors `_convert_to_original_link_type`) and applied it in both matched branches so the original separator style is re-applied and user-written backslashes survive byte-for-byte. Added a backslash-scoped disk-existence guard on the early-exit branch so non-path literals that only coincidentally match a moved directory after normalization are left unchanged (forward-slash targets keep the pure-calculation fast path). 4 regression tests in `test_link_updates.py::TestBug112SeparatorStylePreservedOnDirectoryMove` (all FAILED before fix), manual validation `test/bug-validation/PD-BUG-112_reference_update_separator_corruption_validation.py`, full unit suite green (940 passed) |
| PD-BUG-114 | Relative link not updated when both the referencing and referenced file move in the same operation: the PD-BUG-033 existence guard in `reference_lookup._calculate_updated_relative_path` resolves each outgoing link against the moved file's OLD directory and silently skips it when that path is gone from disk — a false negative when the link's target itself moved in the same operation (both files vacate their old locations before either event is processed). The target's own move event cannot repair it afterwards: processed second, `find_references` misses the stale link (re-indexed from the NEW source dir); processed first, the updater opens the referencing file at its already-vacated old path (Errno 2). Reproduced in both event orders | 2026-08-10 | Move memory + pending recalcs in `ReferenceLookup`: `record_move()` keeps a 300s-TTL map of processed moves (chain-following); the existence guard consults it and rewrites the link to the target's new location on a hit (fragment preserved, `link_target_move_applied` info event); on a miss it registers a pending recalc keyed by resolved target, and `apply_pending_recalcs()` re-runs the moved-file recalc when a later move event explains the key. Handler records moves at the top of `_handle_file_moved` and per-file + dir-level in `_handle_directory_moved` Phase 0; applies pendings after single-file processing and as Phase 1.6 for directory moves. Same-directory early return in `update_links_within_moved_file` removed so in-place renames reach the repair path. 6 regression tests in `test_simultaneousmoves.py` (TE-TST-145; 5 FAILED pre-fix), manual validation `test/bug-validation/PD-BUG-114_simultaneous_move_validation.py`, full suite 1044 passed. Discovered PD-BUG-116 (pre-existing dry-run gap in `update_links_within_moved_file`), filed for triage. WF-004 E2E group flagged Needs Re-execution. **Code Review 2026-08-10 (PF-TSK-005) rejected this first fix** — 1 Major: removing the same-directory early return routed *every* in-place rename through `os.path.relpath`, which returns a canonical path, so links that already resolved correctly were silently reformatted (`./x`→`x`, `..\x`→`../x`, `dir/`→`dir`). Confirmed by running an identical probe against a `git archive HEAD` baseline, and it contradicted the no-write invariant the TDD asserted at the time. **Follow-up fix**: authored-form preservation guard in `_calculate_updated_relative_path` — resolve the link *as authored* against the file's new directory and return it untouched when that already equals the intended target, making recalculation semantic rather than textual. Two review Minors folded in on human approval: `pending_link_recalc_dropped` warning replacing a silent `continue` in `apply_pending_recalcs` (the same silent-skip class this bug exists to fix), and a chain-hop timestamp refresh in `record_move` so a still-moving chain is not aged from its first hop. Regression tests 6→10 (3 new FAILED pre-fix with the exact reformatting signature; a 4th is a cross-directory boundary guard that passes both pre- and post-fix, proving the suppression does not leak); validation script gained a 3rd scenario; TDD 2.2.1 invariant corrected |
| PD-BUG-116 | Dry-run mode did not prevent link rewrites inside moved files: `update_links_within_moved_file` (reference_lookup.py) wrote the recalculated re-depth content via `_write_with_backup` without consulting the updater `dry_run` flag — the gate exists in both `LinkUpdater` write paths but was never added when the within-moved-file pass was extracted into `ReferenceLookup` (which already held `self.updater`). References in other files were correctly gated; only the moved file itself was rewritten in `--dry-run` | 2026-08-10 | Dry-run gate at the write decision: `dry_run_skip` info event (same event name as the updater's), no write, no `.bak`, and `content` reset to the original so `rescan_moved_file_links` indexes the actual on-disk state (DB honesty); `moved_file_links_updated` now logged only in live mode. Test fixture hardening: `mock_updater` pins `dry_run = False` (bare MagicMock attribute is truthy). 3 regression tests in `test_reference_lookup.py` (2 FAILED pre-fix; count 41→44), manual validation `test/bug-validation/PD-BUG-116_dry_run_moved_file_rewrite_validation.py` (dry + live legs), full suite 1085 passed. Write-site sweep confirmed the only dry-run leak. Closed same day via S-scope quick path (no separate Code Review); no E2E re-execution flag — TE-E2E-019 result invariant, now conforming by design |
| PD-BUG-118 | Reference-update pass corrupted content *adjacent* to the matched path span, silently: 181 lines lost a trailing slash and 9 took a compound mangle (leading letter of the next segment doubled, closing backtick or sentence period eaten) across 91 files in one burst. Three independent mechanisms, all reproduced against repo source. **A** — `parsers/python.py` built the docstring text as a separate `inner` string via `_TRIPLE_QUOTE_RE.split(line)` and stored match offsets *into that string* as line columns, so on any line containing triple quotes every column was short by the length of the text preceding the docstring content (7 for a 4-space-indented `"""`); `updater._replace_at_position` then sliced the physical line at those stale columns. **B** — every `path_resolver` rewrite returns `normalize_path()` output and `os.path.normpath` strips a trailing separator, so a directory reference authored `doc/x/` was rewritten `doc/x`. **C** (found by the fix, not the report) — bare-path character classes include `.`, so a path ending a sentence was captured *with* its period, which the rewrite then replaced. Triage premise corrected: the trigger is an ordinary directory move via `handler.py:756`, NOT a self-move — both detectors already guard same-path moves | 2026-08-17 | **A**: docstring extraction now records line-absolute `(start, end)` spans, unifying the triple-quote state machine with span collection so columns are correct by construction (also fixes a latent miss — text *before* a closing `"""` was previously never scanned). **B**: new `PathResolver._apply_trailing_separator_style`, applied once at the `calculate_new_target` choke point so all match branches and the anchor path are covered; sibling of `_apply_separator_style` (PD-BUG-112) and of the `reference_lookup` authored-form guard (PD-BUG-114) whose comment names the `dir/`→`dir` case. Preserves a genuine trailing `..`. **C**: new shared `BaseParser._trim_trailing_punctuation`, wired into the python docstring-dir extractor and the markdown bare-path and @-prefix extractors with `column_end` recomputed from the trimmed length — the post-match form of the trailing-boundary lookahead the markdown standalone pattern already carries (PD-BUG-080). 17 regression tests written failing-first (2 controls passed pre-fix, making the other 15 load-bearing); full suite 1239 passed / 3 skipped / 4 xfailed, identical to baseline; manual validation `test/bug-validation/PD-BUG-118_path_span_corruption_validation.py` 10/10 PASS driving the real service with a real directory move. Cross-parser sweep confirms 0 column drift across all 7 parsers (json/yaml/powershell/dart/generic already added their offsets correctly). B2 (a separate no-op-rewrite guard) was evaluated and dropped as subsumed by B1 — the no-op test passes on B1 alone. Committed inside sweep commit `006f5a7` by a parallel session, so no isolated per-bug commit exists. 36 E2E entries flagged Needs Re-execution |
| PD-BUG-119 | The PD-BUG-114 fix added the first cross-event mutable state on `ReferenceLookup` — `_recent_moves` and `_pending_recalcs` — as plain dicts, while `handler.on_moved` does not serialize move processing. The watchdog observer thread (`_handle_file_moved`) and the `DirectoryMoveDetector` worker thread (`_handle_directory_moved`, a daemon thread started in `dir_move_detector._trigger_processing`) therefore mutate them concurrently. Three interleavings, all confirmed by deterministic reproduction: (1) `_prune_move_memory` iterates both dicts in list comprehensions, so a concurrent insert raises `RuntimeError: dictionary changed size during iteration` — escaping `record_move` it abandons the **entire** move link update (that call sits at the top of the try block), escaping `_lookup_recent_move` it is swallowed by `_calculate_updated_relative_path` and leaves the link silently stale, the exact PD-BUG-114 symptom; (2) `_register_pending_recalc` did `setdefault` then store, so a concurrent `apply_pending_recalcs` pop between the two left the store in an orphaned dict and the deferred repair was lost with no error and no log; (3) `record_move` chain-following was an unguarded read-modify-write that could interleave into a torn chain update — not in the report, found while fixing. Trigger window is bulk restructuring, the primary use case. Found by Code Review (PF-TSK-005) of the PD-BUG-114 fix before release | 2026-08-17 | Single non-reentrant `threading.Lock` (`_move_memory_lock`) guarding every read and write of both dicts, matching the lock-guarded-shared-state pattern every other component already follows (`LinkDatabase`, both move detectors, handler stats, and `PerformanceLogger._timers` under PD-BUG-027 — the same defect class). `record_move` takes prune + chain read-modify-write + record as **one** critical section; `_lookup_recent_move` takes prune + get; `_register_pending_recalc` takes `setdefault` + store as one section with logging outside; `apply_pending_recalcs` holds the lock **only** for the pop and releases it before the repair loop, which does file I/O and re-enters the guarded methods — holding it there would deadlock the daemon, a strictly worse outcome than the race. `_prune_move_memory` is now a caller-holds-the-lock helper that deliberately does not acquire. Rejected `RLock`-wrapping-everything: it removes the reentrancy hazard but serializes both threads through file I/O during exactly the bulk passes this bug targets, buying no correctness. Rejected a handler-wide `on_moved` lock: the detector worker exists precisely so directory processing does not block the observer thread. 5 regression tests in `test_movememorythreadsafety.py` (TE-TST-152; 4 FAILED pre-fix — two with the literal `RuntimeError` inside `_prune_move_memory`'s comprehension), interleavings driven **deterministically** by hooking a callable the production code already invokes inside each vulnerable window rather than by racing; the 5th is a design pin that fails an over-locking fix by deadlocking. Full suite 1252 passed / 3 skipped / 4 xfailed / 0 failures. No manual validation script: a race in internal state has no trigger a human can drive reproducibly, so a clean manual run would prove nothing. No new log events — OB is addressed by removing the silent-failure conditions, not by instrumentation. E2E re-execution flag deliberately NOT applied: all 36 candidate entries were already at Needs Re-execution and the flag would only overwrite their existing reasons. Discovered and filed PF-IMP-2030 (framework: `New-TestFile.ps1` creates the tracking row with a blank Test Cases Count and `Validate-TestTracking.ps1` Check 8 skips non-numeric cells, so `-Fix` cannot repair it and the check passes vacuously) |
| PD-BUG-120 | PD-BUG-118 restored the authored trailing separator only at the `PathResolver.calculate_new_target` choke point, which never sees the links recalculated *inside* a moved file. `reference_lookup._calculate_updated_relative_path` returned raw `os.path.relpath` output, so a directory reference authored `doc/x/` came back as `../doc/x` once the containing file changed directory. The PD-BUG-114 authored-form guard shields only links that still resolve unchanged, so the loss landed exactly on legitimate rewrites - and on a concatenation prefix in a moved `.py`/`.ps1`/`.yaml` (`logs/linkwatcher/`) it silently broke every downstream concatenation with nothing logged | 2026-08-17 | `_apply_trailing_separator_style` promoted from a `PathResolver` static method to shared `utils.apply_trailing_separator_style` and applied at both return points of `_calculate_updated_relative_path` - covering all three producing sites (move-memory relpath branch, move-memory root-destination branch, main branch) - and *before* the `#fragment` is reattached. The restored separator now follows the result own style, falling back to the authored one, so `ReferenceLookup` forward-slash output never gains a mixed `../doc/x\` form; `PathResolver` renders results in the authored style before calling, so its behavior is unchanged. 10 regression tests (8 unit at the producing function, 2 end-to-end driving the real service) plus manual validation `test/bug-validation/PD-BUG-120_trailing_separator_within_moved_file_validation.py` 6/6. Code review approved 2026-08-18: mutation verification turns 8 of 10 red (the 2 green are the intended controls) and also turns PD-BUG-118 `TestTrailingSlashPreservation` red, confirming the promotion kept the older coverage; findings PD-BUG-124 and TD270 filed |

### Tech Debt and Known Limitations

| Item | Type | Reason | Current Mitigation | Priority | Estimated Effort | Future Resolution | Tracked In |
| ---- | ---- | ------ | ------------------ | -------- | ---------------- | ----------------- | ---------- |
| Backup file cleanup | Known Limitation | `.bak` files accumulate over time | Manual cleanup | Low | 1 hour | Add configurable auto-cleanup | — |
| Python src/ layout support | Enhancement | PD-BUG-078: `python_source_root` config strips source root prefix from import path calculations | Auto-read from `project-config.json` `paths.source_code`; override via config file or env var | N/A | Done | — | PD-BUG-078 |

---

## 10. Next Steps

**Last Updated**: 2026-07-15

### Immediate Next Actions

1. ~~Fix the open implementation gap~~ — **done 2026-07-12** (§3 PF-TSK-007 entry; §9 gap RESOLVED).
2. ~~Re-review the fix~~ — **PASSED 2026-07-12** (§3 PF-TSK-005 repeat-cycle entry; 0 findings above Suggestion level) → 🔎 Needs Test Scoping.
3. ~~Performance & E2E Test Scoping (PF-TSK-086)~~ — **done 2026-07-15** (§3 entry): BM-009 perf test (⬜ Needs Creation) + WF-011 workflow/milestone created; feature → 🟢 Completed.
4. **Test Audit (PF-TSK-030)** of the 14 enhancement tests (13 + suffix-hijack regression) — audit gate, must pass before release.
5. **Release & Deployment** after scoping + audit — the deployed daemon lags repo source; the blueprint use case needs the next release before live use. User handbooks reference "v2.2" pending release-number confirmation.
