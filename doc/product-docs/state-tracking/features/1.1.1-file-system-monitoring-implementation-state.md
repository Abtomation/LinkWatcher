---
id: PD-FIS-049
type: Process Framework
category: Feature Implementation State
version: 2.0
created: 2026-02-21
updated: 2026-03-02
feature_name: File System Monitoring
implementation_mode: Retrospective Analysis
feature_id: 1.1.1
status: Retrospective Analysis
consolidates:
  - PF-FEA-008 (1.1.1 Watchdog Integration)
  - PF-FEA-009 (1.1.2 Event Handler)
  - PF-FEA-010 (1.1.3 Initial Scan)
  - PF-FEA-011 (1.1.4 File Filtering)
  - PF-FEA-012 (1.1.5 Real-Time Monitoring)
---

# File System Monitoring - Implementation State

> **📖 Usage guide**: [Feature Implementation State Tracking Guide (PF-GDE-043)](../../../process-framework/guides/04-implementation/feature-implementation-state-tracking-guide.md)
>
> **Retrospective Analysis mode** (onboarding tasks [PF-TSK-064](../../../process-framework/tasks/00-setup/codebase-feature-discovery.md), [PF-TSK-065](../../../process-framework/tasks/00-setup/codebase-feature-analysis.md), [PF-TSK-066](../../../process-framework/tasks/00-setup/retrospective-documentation-creation.md)):
> - Section 3 tracks analysis progress rather than planned tasks
> - Section 5 (Code Inventory) is the primary deliverable — every file must be assigned
> - Section 7 documents decisions discovered in code, not planned decisions
> - All content is descriptive ("what is") rather than prescriptive ("what should be")

> **v2.0 Consolidation**: This file replaces 5 former feature states (PF-FEA-008 through PF-FEA-012, old 1.1.1–1.1.5) as part of the 42→9 feature consolidation. See [Feature Consolidation State](../../../process-framework/state-tracking/temporary/old/feature-consolidation-state.md).

---

## 1. Feature Overview

### Feature Description

File System Monitoring is the event-driven core of LinkWatcher, responsible for detecting file movements and triggering link updates. The subsystem is implemented across four modules (refactored from a single God Class via TD005/TD022/TD035):
- **`linkwatcher/handler.py`** — `LinkMaintenanceHandler`: Central event dispatcher extending watchdog's `FileSystemEventHandler`, delegates to specialized modules for move detection, reference management, and link updates (474 LOC)
- **`linkwatcher/move_detector.py`** — `MoveDetector`: Per-file move detection state machine (delete+create correlation with priority-queue expiry via single worker thread — TD107)
- **`linkwatcher/dir_move_detector.py`** — `DirectoryMoveDetector`: Batch directory move detection state machine (3-phase: detect, confirm, apply)
- **`linkwatcher/reference_lookup.py`** — `ReferenceLookup`: Reference lookup, DB management, link content updates in moved files, and directory move per-file processing (517 LOC)

The handler implements two move detection mechanisms: (1) **Per-file**: when a file is deleted, it starts a 10-second timer; if a file with the same basename is created within that window, the pair is classified as a move operation. (2) **Batch directory** (PD-BUG-019 fix): when a directory is deleted on Windows (where watchdog fires individual file events instead of `DirMovedEvent`), all known files are buffered in a `_PendingDirMove` entry; incoming `file_created` events are matched against the buffer using path subtraction, and processing runs on a separate thread when all files are matched or a settle timer fires. A 4-tuple deduplication key (event_type, src_path, dest_path, timestamp) prevents processing duplicate events from the OS. The handler only processes `on_created`, `on_deleted`, and `on_moved` events — `on_modified` is intentionally ignored as link content changes don't affect file references.

Initial scanning uses `os.walk()` with directory pruning (skipping `.git`, `node_modules`, `__pycache__`, etc.) to populate the `LinkDatabase` on startup. File filtering uses extension-based inclusion (`.md`, `.yaml`, `.json`, `.py`, `.dart`, etc.) combined with directory exclusion patterns.

The real-time monitoring is managed by the service via a `while True` / `time.sleep()` loop with `try/finally` cleanup ensuring the watchdog Observer is always stopped. Five startup scripts (PowerShell and batch) provide convenient launch mechanisms.

### Business Value

- **User Need**: File movements must be detected instantly and automatically, without user intervention.
- **Business Goal**: Zero-configuration real-time monitoring that "just works" when started.
- **Success Metrics**: File moves detected within 10 seconds; no missed events; clean startup and shutdown.

### Scope

**In Scope**:

- Watchdog Observer integration with lazy creation and `recursive=True` scheduling
- `LinkMaintenanceHandler` event processing (on_created, on_deleted, on_moved)
- Timer-based move detection with 10-second pairing window
- 4-tuple event deduplication
- Initial directory scan via `os.walk()` with directory pruning
- Extension-based file filtering and directory exclusion
- Real-time monitoring loop with cleanup
- Startup scripts (PowerShell/batch)

**Out of Scope**:

- Service orchestration (→ 0.1.1 Core Architecture)
- Link parsing during scan (→ 2.1.1 Link Parsing System)
- Link updating after move detection (→ 2.2.1 Link Updating)
- Link storage (→ 0.1.2 In-Memory Link Database)

---

## 2. Current State Summary

**Last Updated**: 2026-03-13
**Current Status**: MAINTAINED (retrospective analysis — pre-framework implementation)
**Current Task**: None — Enhancement PF-STA-053 (directory-path reference updates) complete
**Completion**: 100% (fully deployed, in production use)

### What's Working

- [✓] Watchdog Observer with recursive file system monitoring
- [✓] Per-file timer-based move detection (delete+create pairing within 10s)
- [✓] Batch directory move detection via delete+create correlation (PD-BUG-019 fix)
- [✓] Directory-path reference updates during directory moves (PF-STA-053 enhancement)
- [✓] Outward-pointing link recalculation within moved directories (PD-BUG-039 fix)
- [✓] Co-moved file awareness during directory move cleanup (PD-BUG-038 fix)
- [✓] 4-tuple event deduplication
- [✓] Initial scan via os.walk with directory pruning
- [✓] Extension-based file filtering
- [✓] Real-time monitoring loop with clean shutdown
- [✓] 5 startup scripts for convenient launching

### What's In Progress

_Nothing in progress._

### What's Blocked

_Nothing blocked._

### Bug Fix History

| Bug ID | Date | Description | Status |
| --- | --- | --- | --- |
| PD-BUG-019 | 2026-02-26 | Batch directory move detection — replaces per-file timers for directory moves with 3-phase batch approach | ✅ Verified |
| PD-BUG-035 | 2026-03-16 | File replacement detection — _process_true_file_delete checks file existence before processing; rescans replaced files instead of removing links | ✅ Verified |
| PD-BUG-038 | 2026-03-16 | Co-moved file awareness — process_directory_file_move excludes co-moved files from reference updates and cleanup rescans; defers rescan to handler's second loop | 🧪 Fixed |
| PD-BUG-053 | 2026-03-30 | File move during startup scan not detected — reordered start() to start observer before _initial_scan(), closing gap where moves were invisible | 🧪 Fixed |

---

## 3. Implementation Progress

### Retrospective Analysis Progress

> **Note**: This feature was implemented before the process framework was established.

- [✅] **PF-TSK-064**: Codebase Feature Discovery — Code Inventory populated (2026-02-17)
- [✅] **PF-TSK-065**: Codebase Feature Analysis — Dependencies and Design Decisions documented (2026-02-18)
- [✅] **PF-TSK-066**: Retrospective Documentation Creation — Tier Assessment (ART-ASS-194), FDD (PD-FDD-024), TDD (PD-TDD-023) created (2026-02-19/20)
- [✅] **Feature Consolidation**: Merged 1.1.1–1.1.5 into unified feature, new state file created (2026-02-21)
- [✅] **PF-TSK-007**: Bug Fixing — PD-BUG-019 batch directory move detection fix, TDD updated (2026-02-26)
- [✅] **PF-TSK-022**: Code Refactoring — TD005 God Class decomposition: handler.py split into 3 modules (handler.py + move_detector.py + dir_move_detector.py), TD009 duplicated stale retry resolved (2026-03-02)
- [✅] **PF-TSK-022**: Code Refactoring — TD018: MoveDetector timers now tracked and cancelled on match/re-buffer (PF-REF-032) (2026-03-02)
- [✅] **PF-TSK-022**: Code Refactoring — TD022: Extracted 7 reference-lookup methods from handler.py into reference_lookup.py (PF-REF-042). Handler 873→681 lines (2026-03-03)
- [✅] **PF-TSK-022**: Code Refactoring — TD107: Replaced N timer threads with single worker thread + heapq priority queue in MoveDetector (PD-REF-106) (2026-03-27)
- [✅] **PF-TSK-022**: Code Refactoring — TD035: Extracted `_update_links_within_moved_file`, `_calculate_updated_relative_path`, and `_handle_directory_moved` per-file loop into reference_lookup.py (PF-REF-046). Handler 681→474 lines (2026-03-03)
- [✅] **PF-TSK-068**: Feature Enhancement — PF-STA-053: Directory-path reference updates. Directory moves now also update references to the directory path itself (e.g., quoted directory strings in PowerShell scripts). Added `get_references_to_directory()` to database.py, `find_directory_path_references()` and `cleanup_after_directory_path_move()` to reference_lookup.py, Phase 2 in handler.py `_handle_directory_moved()`. 7 new unit tests. (2026-03-13)
- [✅] **PF-TSK-007**: Bug Fixing — PD-BUG-038: Co-moved file awareness during directory moves. Added `co_moved_old_paths` to `process_directory_file_move()` and `skip_files` to `cleanup_after_file_move()`. Prevents errors from trying to update/rescan co-moved files at old paths; defers rescan to handler's `_update_links_within_moved_file` loop. 2 regression tests. (2026-03-16)
- [✅] **PF-TSK-068**: Feature Enhancement — PF-STA-058: Parent directory reference updates. When subdirectories move, references to parent/ancestor directory paths are now also updated via prefix replacement. Added `find_parent_directory_references()` to reference_lookup.py, Phase 3 in handler.py `_handle_directory_moved()`. 3 new integration tests + 1 test expectation update. FDD (PD-FDD-024) FR-2/BR-5 and TDD (PD-TDD-023) amended. (2026-03-16)
- [✅] **PF-TSK-007**: Bug Fixing — PD-BUG-053: Startup scan gap. Reordered `start()` to start observer before `_initial_scan()`, closing window where file moves during startup were invisible. 1 regression test. (2026-03-30)

---

## 4. Documentation Inventory

### Design Documentation

| Document | Type | Status | Location | Last Updated |
| -------- | ---- | ------ | -------- | ------------ |
| Tier Assessment (ART-ASS-194) | Tier Assessment | Complete | [ART-ASS-194](../../documentation-tiers/assessments/PD-ASS-194-1-1-1-file-system-monitoring.md) | 2026-02-20 |
| FDD (PD-FDD-024) | Functional Design | Complete | [fdd-1-1-1-file-system-monitoring.md](../../functional-design/fdds/fdd-1-1-1-file-system-monitoring.md) | 2026-02-19 |
| TDD (PD-TDD-023) | Technical Design | Complete | [tdd-1-1-1-file-system-monitoring-t2.md](../../technical/architecture/design-docs/tdd/tdd-1-1-1-file-system-monitoring-t2.md) | 2026-03-02 |

### Existing Project Documentation

| Document | Type | Relevant Content | Confirmed | Notes |
| -------- | ---- | ---------------- | --------- | ----- |
| HOW_IT_WORKS.md | Architecture Overview | Handler module, event flow, move detection | **File Removed** | Was: core monitoring flow |
| [file-type-quick-fix.md](../../user/handbooks/file-type-quick-fix.md) | Troubleshooting | Quick fix for file type filtering issues | Confirmed | Filtering-specific |
| [troubleshooting-file-types.md](../../user/handbooks/troubleshooting-file-types.md) | Troubleshooting | Detailed file type monitoring troubleshooting | Confirmed | Comprehensive filtering guide |

### Quick Links

- **Main Design**: [TDD (PD-TDD-023)](../../technical/architecture/design-docs/tdd/tdd-1-1-1-file-system-monitoring-t2.md)
- **Related Features**: [0.1.1 Core Architecture](0.1.1-core-architecture-implementation-state.md), [0.1.2 In-Memory Link Database](0.1.2-in-memory-link-database-implementation-state.md)

---

## 5. Code Inventory

### Files Created by This Feature

| File Path | Purpose | Key Components | Status | Created |
| --------- | ------- | -------------- | ------ | ------- |
| [linkwatcher/handler.py](../../../../linkwatcher/handler.py) | Central event coordinator, link update pipeline, scanning, filtering | `LinkMaintenanceHandler` — extends `FileSystemEventHandler`, delegates to MoveDetector, DirectoryMoveDetector, and ReferenceLookup, os.walk scan, file filtering (474 lines after TD035) | Existing | Pre-framework |
| [linkwatcher/reference_lookup.py](../../../../linkwatcher/reference_lookup.py) | Reference lookup, DB management, and link updates | `ReferenceLookup` — multi-format path lookup, stale retry, DB cleanup, file rescanning, link content updates in moved files, directory move per-file processing (517 lines after TD035) | Existing | TD022/TD035 (2026-03-03) |
| [linkwatcher/move_detector.py](../../../../linkwatcher/move_detector.py) | Per-file move detection state machine | `MoveDetector` — delete+create correlation with heapq priority queue + single worker thread, lazy cancellation on match, callback-based communication (198 lines) | Existing | TD005 (2026-03-02), TD107 (2026-03-27) |
| [linkwatcher/dir_move_detector.py](../../../../linkwatcher/dir_move_detector.py) | Batch directory move detection state machine | `DirectoryMoveDetector` + `_PendingDirMove` — 3-phase pipeline (detect, confirm, apply), callback-based (409 lines) | Existing | TD005 (2026-03-02) |
| [LinkWatcher_run/start_linkwatcher.ps1](../../../../LinkWatcher_run/start_linkwatcher.ps1) | PowerShell startup script | Launches LinkWatcher in foreground | Existing | Pre-framework |
| [LinkWatcher_run/start_linkwatcher_background.ps1](../../../../LinkWatcher_run/start_linkwatcher_background.ps1) | PowerShell background startup | Launches LinkWatcher in background | Existing | Pre-framework |
| [LinkWatcher_run/start_linkwatcher.bat](../../../../LinkWatcher_run/start_linkwatcher.bat) | Batch startup script | Windows batch launcher | Existing | Pre-framework |

### Test Files

| Test File | Type | Coverage Areas | Status | Created |
| --------- | ---- | -------------- | ------ | ------- |
| test/automated/unit/test_handler.py | Unit | Event handling, move detection, filtering | **Not Found on Disk** | Pre-framework |
| [test/automated/integration/test_comprehensive_file_monitoring.py](../../../../test/automated/integration/test_comprehensive_file_monitoring.py) | Integration | End-to-end file monitoring | Existing | Pre-framework |

---

## 6. Dependencies

### Feature Dependencies

**This Feature Depends On**:

- **[PF-FEA-046: Core Architecture](0.1.1-core-architecture-implementation-state.md)** (MAINTAINED)
  - Why: Uses `FileOperation` model and `normalize_path()`, `should_monitor_file()` utilities
  - Impact if unavailable: Cannot represent events or filter files

- **[PF-FEA-047: In-Memory Link Database](0.1.2-in-memory-link-database-implementation-state.md)** (MAINTAINED)
  - Why: Queries database for links to moved files during event handling
  - Impact if unavailable: Cannot find references to update

- **[PF-FEA-050: Link Parsing System](./2.1.1-link-parsing-system-implementation-state.md)** (MAINTAINED)
  - Why: Uses parser during initial scan to discover links
  - Impact if unavailable: Initial scan cannot populate database

- **[PF-FEA-051: Link Updating](2.2.1-link-updating-implementation-state.md)** (MAINTAINED)
  - Why: Triggers updater when a move is detected
  - Impact if unavailable: Moves detected but links not updated

**Other Features Depend On This**:

- **[PF-FEA-046: Core Architecture](0.1.1-core-architecture-implementation-state.md)** (MAINTAINED)
  - Why: Service orchestrator creates and manages the handler and Observer

### System Dependencies

**Required Packages**:

| Package | Version | Purpose | Added |
| ------- | ------- | ------- | ----- |
| watchdog | ≥2.0 | File system event delivery via Observer pattern | Pre-framework |

---

## 7. Design Decisions

### Decision 1: Timer-Based Move Detection

**Date**: Pre-framework (discovered in code)
**Context**: Many file systems report moves as separate delete+create events rather than a single move event. The handler must pair these events to detect moves.

**Decision Made**: 10-second timer-based pairing — on delete, start timer; if matching create arrives within 10s, classify as move

**Rationale**: Watchdog's `on_moved` is unreliable across all platforms and file operations (e.g., cut/paste across volumes). The delete+create pairing approach works universally.

**Implications**:

- 10-second delay before move processing (trade-off for reliability)
- Rapid successive moves of the same file may not be detected correctly
- Timer thread management adds complexity

---

### Decision 2: 4-Tuple Event Deduplication

**Date**: Pre-framework (discovered in code)
**Context**: Operating systems and watchdog may deliver duplicate events for the same file operation.

**Decision Made**: Deduplicate using (event_type, src_path, dest_path, timestamp) as a compound key

**Rationale**: Prevents processing the same event multiple times. The 4-tuple uniquely identifies each event with sufficient granularity.

---

### Decision 3: Ignore on_modified Events

**Date**: Pre-framework (discovered in code)
**Context**: Watchdog delivers `on_modified` events for content changes. LinkWatcher only cares about file moves.

**Decision Made**: Only process `on_created`, `on_deleted`, `on_moved` — ignore `on_modified` entirely

**Rationale**: Content changes don't affect file references. Processing modification events would add unnecessary overhead and could trigger false update cycles (e.g., when the updater writes to a file, it generates a modification event).

---

### Decision 4: os.walk with Directory Pruning for Initial Scan

**Date**: Pre-framework (discovered in code)
**Context**: Initial scan needs to discover all monitorable files while skipping irrelevant directories.

**Decision Made**: `os.walk()` with in-place modification of `dirs` list to prune `.git`, `node_modules`, `__pycache__`, `venv`, etc.

**Rationale**: Efficient — pruned directories are never entered. In-place `dirs` modification is the standard Python pattern for os.walk pruning.

---

### Decision 5: Lazy Observer Creation

**Date**: Pre-framework (discovered in code)
**Context**: The watchdog Observer is resource-intensive and should only be created when monitoring actually starts.

**Decision Made**: Observer is created lazily in `start()`, not in `__init__()`

**Rationale**: Allows the service to be instantiated for testing or configuration purposes without starting actual file system monitoring.

---

### Implementation Patterns Used

**Event Handling Pattern**:

- Pattern: FileSystemEventHandler subclass delegating to specialized detector modules via callbacks
- Why: Reliable cross-platform move detection with separation of concerns (TD005 decomposition)
- Where: `linkwatcher/handler.py` (coordinator), `linkwatcher/move_detector.py` (per-file), `linkwatcher/dir_move_detector.py` (batch directory)

**Scanning Pattern**:

- Pattern: os.walk with directory pruning
- Why: Efficient recursive directory traversal with exclusions
- Where: `LinkMaintenanceHandler.initial_scan()`

---

## 8. Issues & Resolutions Log

### Tech Debt and Known Limitations

| Item | Type | Reason | Current Mitigation | Priority | Estimated Effort | Future Resolution | Tracked In |
| ---- | ---- | ------ | ------------------ | -------- | ---------------- | ----------------- | ---------- |
| TD004: File filtering config not wired | Tech Debt | Extension list and directory exclusions are hardcoded, not driven by config | Hardcoded values work for current use cases | Medium | 2 hours | Wire filtering config from LinkWatcherConfig | technical-debt-tracking.md |
| 10-second move detection delay | Architectural Constraint | Timer-based pairing requires waiting for potential create event | Acceptable latency for the use case | Low | N/A | Could reduce timer but risks missed pairings | — |

---

## 9. Next Steps

**Last Updated**: 2026-03-02

### Immediate Next Actions

1. **Wire file filtering to configuration system**
   - **Why**: TD004 — filtering is hardcoded instead of config-driven
   - **How**: Read extension list and directory exclusions from `LinkWatcherConfig`
   - **Estimate**: Low (2 hours)

### Recently Completed

- **PF-STA-058 Parent directory reference updates** (2026-03-16): When subdirectories move, references to parent/ancestor directory paths are also updated via prefix replacement. Added Phase 3 to `_handle_directory_moved()`, `find_parent_directory_references()` to reference_lookup.py. 3 new integration tests. FDD-024 FR-2/BR-5 and TDD-023 amended.
- **PF-STA-053 Directory-path reference updates** (2026-03-13): Directory moves now also update references to the directory path itself in other files. Added Phase 2 to `_handle_directory_moved()`, new methods in database.py and reference_lookup.py, 7 new unit tests. FDD-024 and TDD-023 amended.
- **PD-BUG-010 Title preservation** (2026-03-03): handler.py `_update_links_within_moved_file` regex now preserves markdown link title attributes during internal link updates
- **TD107 Priority queue threading** (2026-03-27): MoveDetector replaced N timer threads with single worker thread + heapq priority queue — O(1) threads for any number of pending deletes (PD-REF-106)
- **TD018 Timer tracking** (2026-03-02): MoveDetector timers now tracked in `_timers` dict, cancelled on match/re-buffer, marked daemon (PF-REF-032)
- **TD005 God Class decomposition** (2026-03-02): handler.py decomposed from 1281 → 839 lines into 3-module architecture
- **TD009 Duplicated stale retry** (2026-03-02): Resolved alongside TD005 via shared `_retry_stale_references()` method
- **PD-BUG-019 verification** (2026-03-02): Batch directory move detection verified working
