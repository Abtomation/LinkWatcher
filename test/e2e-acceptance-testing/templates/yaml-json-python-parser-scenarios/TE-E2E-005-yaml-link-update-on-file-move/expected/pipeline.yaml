# Pipeline configuration
pipeline:
  stages:
    build:
      source: moved/settings.conf
      output: data/schema.sql

defaults: &defaults
  config_file: moved/settings.conf
  schema: data/schema.sql

production:
  <<: *defaults
